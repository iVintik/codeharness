# Session Issues Log — 2026-03-19 (session 7)

### 0-1-sprint-state-live-updates — create-story (2026-03-19T09:40Z)

- **Ambiguity: CLI command vs inline writes.** harness-run.md is a prompt, not compiled code. Dev agent must decide: CLI subcommand, node -e inline, or direct JSON write.
- **AC #4 already satisfied** by existing atomic write pattern in state.ts.
- **Backward compatibility risk:** New fields in SprintState.run need graceful defaults for old state files.

### 0-1-sprint-state-live-updates — dev-story (2026-03-19T09:45Z)

- **Pre-existing test isolation issue**: Sprint module tests fail when run together due to shared process.cwd() and sprint-state.json file. Pass individually.
- **No backward compatibility merge in getSprintState**: Old state files missing new fields will have undefined, not null. updateRunProgress and clearRunProgress handle this correctly.
- **No validation on --phase CLI input**: Accepts any string, not validated against union type at runtime.
- **sprint-status.yaml overwritten with test data**: Dev agent tests wrote to real state file. Manually restored.
- **session-issues.md overwritten with test data**: Same issue. Manually restored.
