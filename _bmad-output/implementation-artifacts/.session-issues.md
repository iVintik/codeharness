# Session Issues Log — 2026-04-03

### 16-1-hierarchical-flow-schema-parser — create-story (2026-04-03T16:39)

- Config file at `_bmad/config.yaml` not `_bmad/bmm/config.yaml` as workflow implied
- No `project-context.md` exists in repo — skipped
- Decomposed bundled epic ACs into 11 separate ACs for testability
- Added ACs #9 (flow/story_flow mutual exclusivity), #10 (agent: null), #11 (scope: per-epic) — implied by architecture but not explicit in epic
- Risk: `additionalProperties: false` in schema requires careful extension
- Risk: `oneOf` for flow/story_flow mutual exclusivity may be complex with Ajv — simpler to validate in TypeScript
- All 11 ACs tagged `test-provable`

Token Report: 23 tool calls (Bash: 6, Read: 10, Glob: 7, Write: 1, Skill: 1). 12 unique files, 12 total reads. No redundancy.

### 16-1-hierarchical-flow-schema-parser — dev-story (2026-04-03T16:48)

- Pre-existing test failure: `commands/__tests__/stats.test.ts` — unrelated to this story
- Pre-existing TS errors in `run.test.ts`, `issue.test.ts`, `verdict-parser.test.ts` — unrelated
- Used Python script for bulk-replace of ~60 inline workflow objects in `workflow-engine.test.ts` with `makeWorkflow()` wrappers
- Schema vs code enforcement: `flow`/`story_flow` mutual exclusivity enforced in parser code, not JSON Schema `oneOf` (draft-07 creates confusing errors)
- `execution` object has `additionalProperties: false` — future config additions require schema changes
- 4676 tests passed, 1 pre-existing failure

Token Report: 33 tool calls (Bash: 10, Read: 11, Edit: 15, Grep: 5, Write: 2, Glob: 2, Skill: 1). 12 unique files, 14 total reads. No redundancy.

### 16-1-hierarchical-flow-schema-parser — code-review (2026-04-03T16:57)

- HIGH: `resolveHierarchicalFlow` accepted arbitrary types without validation — could produce malformed objects. Fixed with `resolveExecutionConfig()` and `normalizeFlowArray()`.
- MEDIUM: Operator precedence issue in `workflow-parser.ts` with `as` cast + `??`. Fixed.
- MEDIUM: `run.ts` null guard used `!== null` instead of `!= null`. Fixed.
- MEDIUM: 4 missing test cases for loop blocks and strategy validation error paths. Added.
- LOW (not fixed): Duplicate `flow`/`story_flow` coexistence check in two files (defense-in-depth).
- LOW (not fixed): `resolveHierarchicalFlow` silently returns empty storyFlow when no flow provided.
- `codeharness coverage --min-file 80` command is broken (pre-existing coverage parser bug).
- Tests: 28 → 43 (+15 new). 4691 passed, 1 pre-existing failure.

Token Report: 28 tool calls (Bash: 16, Read: 8, Edit: 6, Grep: 2, Glob: 2, Skill: 1). 10 unique files. ~4 redundant Bash calls trying to parse coverage output.

### 16-1-hierarchical-flow-schema-parser — verify (2026-04-03T17:05)

- ALL_PASS: 11/11 ACs verified with test evidence
- `codeharness verify` initially failed on preconditions: AGENTS.md missing `hierarchical-flow.ts`, proof format headers wrong (`### AC #N:` vs `## AC N:`)
- Fixed: AGENTS.md entries for `hierarchical-flow.ts` and `drivers.ts`, proof header format, session state flags
- Pre-existing: `stats.test.ts` failure, lint 1 error + 52 warnings (all outside story scope)
- Coverage: `hierarchical-flow.ts` at 100% across all metrics
- Redundancy: Coverage run executed 3 times with different grep filters before usable output

Token Report (verify subagent): 17 tool calls (Bash: 11, Read: 3, Grep: 3, Write: 1). 4 unique files. Coverage run redundancy.
Token Report (fix subagent): ~46 tool calls. Fixed AGENTS.md, proof format, state flags.
