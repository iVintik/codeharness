# Session Issues Log — 2026-03-18 (Session 3)

### 1-3-migrate-existing-tests-to-module-structure — create-story (2026-03-18T04:50:00Z)

- Old story file `1-3-init-command-full-harness-initialization.md` exists from archive-v1 sprint — no conflict since new file uses different name
- Tests being moved without source files (source migration in Story 4.1) — imports will point deeper temporarily
- `src/modules/verify/__tests__/` will temporarily contain both stub tests (from 1.2) and real implementation tests — intentional per phased migration
- All 3 ACs are CLI-verifiable, straightforward

### 1-3-migrate-existing-tests-to-module-structure — dev-story (2026-03-18T04:55:00Z)

None.

### 1-3-migrate-existing-tests-to-module-structure — code-review (2026-03-18T05:00:00Z)

- MEDIUM: Missing AGENTS.md for verify module — created
- MEDIUM: Missing showboat proof document — created at verification/
- MEDIUM: Story referenced nonexistent exec-plan — updated story to note not required for migration
- LOW: verify-prompt.test.ts placement in verify module is architecturally questionable (tests a template, not a module file)
- LOW: Noisy stderr from CLI parser tests (harmless)
- Coverage: src/lib/coverage.ts at 88.94%, src/lib/verify.ts at 89.04% — above floor but lowest
- Architecture: migrated tests still import from ../../../lib/ not from module index — expected tech debt until source extraction

### 2-1-sprint-state-module-unified-state-file — create-story (2026-03-18T05:08:00Z)

- Ambiguity: YAML parsing approach — architecture doc says replace sprint-status.yaml but doesn't specify whether to add a YAML dependency. Recommended manual line-by-line parsing since format is flat key: value.
- Ambiguity: Migration trigger — epic says "first access" triggers migration. Interpreted as: migration runs when sprint-state.json does not exist AND at least one old file exists.
- Risk: Ralph race condition — Ralph (bash) writes ralph/status.json and .story_retries while this module reads them for migration. Migration should only run when ralph is stopped.
- All 8 ACs tagged cli-verifiable (local file I/O and TypeScript functions only).

### 2-1-sprint-state-module-unified-state-file — dev-story (2026-03-18T05:15:00Z)

- Test isolation: tests mock migration.js to prevent migration from running against real project files. Migration tests save/restore original old files in beforeEach/afterEach.
- Concurrent write testing limited: AC #6 simulates sequential writes, not true multi-process concurrency. True fork-based testing felt out of scope for unit tests.
- EMPTY_STORY const spread pattern: safe with readonly fields but fragile if someone bypasses TypeScript checks.
- chmod test for permission errors may not fail on all systems (root user can still read).

### 2-1-sprint-state-module-unified-state-file — code-review (2026-03-18T05:22:00Z)

- HIGH: lastAttempt overwritten on every status change instead of only on in-progress — semantic bug fixed
- HIGH: Test coverage gaps on error paths in writeStateAtomic and parseRalphStatus — tests added
- MEDIUM: DRY violation with duplicated computeSprintCounts logic — extracted to shared function
- MEDIUM: Mutable shared EMPTY_STORY object — replaced with emptyStory() factory function
- MEDIUM: CJS require() in ESM test file — replaced with helper
- MEDIUM: NFR18 violation — state.test.ts exceeded 300 lines, compacted to 287
- LOW (not fixed): process.cwd() used directly as project root detection — fragile
- LOW (not fixed): getSprintState() silently swallows migration errors, falls through to defaultState()
- LOW (not fixed): No schema validation on JSON.parse result in getSprintState
- Coverage: outer catch in migrateFromOldFormat uncovered (LOW risk, inner parsers handle errors)
