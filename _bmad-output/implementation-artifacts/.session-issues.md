# Session Issues Log — 2026-03-16

### 1-2-core-libraries-state-stack-detection-templates — verification (2026-03-16T22:30Z)

- **Verifier permission issue**: `claude --print` without `--dangerously-skip-permissions` is blocked on docker exec and file writes. Required adding the flag.
- **Showboat re-execution mismatch**: Original proof had non-self-contained bash blocks that depended on state created by prior verifier commands. When Showboat re-executed them, the state was gone. Had to rewrite all bash blocks to be fully self-contained (each creates its own temp dir and state).
- **Init fails in container**: `codeharness init` fails because `beads` (Python dep) can't be installed — no Python runtime in Docker image. This is a recurring pattern for all verification runs.
- **AC8 escalated**: Unit test coverage cannot be verified black-box (test files not in npm package). This is the correct outcome — unit test ACs are fundamentally white-box.
- **Stale verify container**: Found a leftover `codeharness-verify` container from a previous session. The cleanup step must have failed or been skipped.
- **Observability stack check**: `codeharness status --check-docker` doesn't detect the shared stack (looks for wrong container names). The shared stack was running fine on the correct ports.

### 1-3-init-command-full-harness-initialization — verification (2026-03-16T22:40Z)

- **3 FAIL verdicts in proof**: AC3 (Docker check is warning not failure), AC4 (state file never created due to beads abort), AC5 (docs scaffold never persisted, JSON falsely reports "created"). These are real code bugs, not verification issues.
- **2 PARTIAL verdicts**: AC2 (`--no-observability` flag removed, replaced with remote endpoint flags), AC7 (enforcement missing observability field).
- **beads as hard dependency**: Init aborts entirely when beads install fails. This gates state file creation, documentation scaffold, and other init steps. Beads unavailability in Docker container reveals this design issue.
- **JSON documentation bug**: Init JSON output reports `"agents_md":"created"` and `"docs_scaffold":"created"` even when files don't exist (because init aborted after reporting but before committing).
- **Showboat timing non-determinism**: AC8's timing bash block was non-reproducible. Replaced with `timeout` guard approach.
- **Proof exit code fix**: AC3's exit code was recorded as 0 by verifier but actual is 1 (PIPESTATUS vs head interaction).
