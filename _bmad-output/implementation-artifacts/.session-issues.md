# Session Issues Log — 2026-03-16

### 1-1-project-scaffold-cli-entry-point — verification (2026-03-16T22:10)

- **Defect found: `state` command visible in --help** (Low) — Commander.js v14 has no `hideHelp()` method. Fixed via `_hidden = true` private property. Works but relies on internal API.
- **Defect found: `onboard --json` ignores JSON flag on early exit** (Medium) — All 5 precondition early-exit paths in onboard.ts used `fail()` instead of `jsonOutput()`. Fixed all 5 instances.
- First verifier run failed due to missing `--dangerously-skip-permissions` flag on `claude --print` — the verifier couldn't execute any commands.
- Test count dropped from 52 files/1613 tests to 51 files/1598 tests between verification runs — needs investigation (likely pre-existing, not caused by this session's changes).

### 1-2-core-libraries-state-stack-detection-templates — verification (2026-03-16T22:20)

- 7/8 ACs passed, 1 escalated (AC8 — can't run vitest in npm-installed package, tests not included)
- **Minor deviations noted by verifier (not failures but worth tracking):**
  - `enforcement.observability` missing from default state (story spec includes it)
  - `coverage.target` defaults to 90 instead of 100 (story spec says 100)
  - `renderTemplate()` doesn't exist in the bundle (story specified it)
- Showboat verify errored on block 8 comparison — expected/actual appear identical (whitespace/encoding issue in Showboat itself)
- Story blocked due to escalated AC8 — running vitest requires source tree not present in black-box container

### 0-1-sprint-execution-skill — verification (2026-03-16T21:10)

- 5/6 ACs escalated — this is a markdown skill (Claude Code slash command), not a CLI feature. ACs 2-4 require live Claude Code session with Agent tool. AC5 partially verified (retry tracking works, end-to-end retry needs live session).
- Shared VictoriaMetrics stack was running but per-project docker-compose.harness.yml conflicted (port collision). Had to use shared stack directly.
- Showboat verify errored on block 39 ("exec: no command") — proof formatting issue, but JSON proof quality parsed correctly (pending=0, escalated=5).
- Story blocked due to integration-required ACs — these ACs are inherently not black-box verifiable since they test Claude Code session behavior.
- Observation: Stories that are pure markdown skills (no TypeScript) will always have most ACs escalated in black-box verification. This is a structural limitation — consider a different verification approach for skill-only stories.
