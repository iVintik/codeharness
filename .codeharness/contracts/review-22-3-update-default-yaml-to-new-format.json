{
  "version": 1,
  "taskName": "review",
  "storyId": "22-3-update-default-yaml-to-new-format",
  "driver": "codex",
  "model": "claude-sonnet-4-6",
  "timestamp": "2026-04-05T17:49:18.998Z",
  "cost_usd": null,
  "duration_ms": 118401,
  "changedFiles": [],
  "testResults": null,
  "output": "I’m reviewing story `22-3-update-default-yaml-to-new-format` as a code review task, starting from the current diff and the files that implement workflow parsing and initialization so I can check correctness, backward compatibility, and AC coverage before I give a pass/fail verdict.I’ve identified the implementation surface: the template, parser, execution wiring, schema, and two test files. Next I’m reading the actual diffs and validating the new format against the acceptance criteria, especially the backward-compat and init-path requirements.The parser and schema changes are straightforward, but I need to confirm the runtime actually consumes `GateBlock`. If the template now emits gates and the executor still only understands plain tasks and `for_each`, that’s a functional regression regardless of parser coverage.I’ve confirmed the main architectural risk: the parser accepts the new `workflow:` tree, but the execution stack still drives exclusively off `storyFlow` and `epicFlow`. I’m checking whether anything compiles that tree into the engine before I finalize the findings, because if not the new default template makes `run` a no-op.I have enough context for the primary correctness finding. I’m pulling exact line numbers now and checking whether AC8 and runtime behavior are actually covered by tests, so the verdict can distinguish implementation bugs from missing verification.There’s a second compatibility angle I’m checking: old standalone workflows still validate, but old patch files that `extends: embedded://default` may now merge into a mixed-format object and start failing. That would violate the migration intent even if AC7’s narrow parser case passes.<verdict>fail</verdict>\n\n<issues>\n1. High: The new default template is not executable because the runtime still drives entirely from `storyFlow` and `epicFlow`, while the parser returns both as empty arrays for `workflow:` files. [`parseWorkflowData()`](/Users/ivintik/dev/personal/codeharness/src/lib/workflow-parser.ts#L200) returns `storyFlow: []` and `epicFlow: []` for the new format, and both [`runWorkflowActor()`](/Users/ivintik/dev/personal/codeharness/src/lib/workflow-runner.ts#L103) and the XState engine in [`workflow-machines.ts`](/Users/ivintik/dev/personal/codeharness/src/lib/workflow-machines.ts#L203) only execute `storyFlow`/`epicFlow`. The CLI/TUI layer also renders only those legacy arrays in [`run.ts`](/Users/ivintik/dev/personal/codeharness/src/commands/run.ts#L286). With the updated [`templates/workflows/default.yaml`](/Users/ivintik/dev/personal/codeharness/templates/workflows/default.yaml), `codeharness run` will effectively have no workflow steps to execute. This is both a correctness bug and an architecture violation: the default config was migrated ahead of the execution engine that consumes it.\n\n2. Medium: Backward compatibility is incomplete for existing user patch workflows. [`resolveWorkflow()`](/Users/ivintik/dev/personal/codeharness/src/lib/workflow-resolver.ts#L126) always starts from the embedded default and deep-merges user/project patches onto it. Since the embedded default now uses `workflow:`, any legacy patch that still sets `story_flow`/`epic_flow` will merge into a mixed-format object, and [`parseWorkflowData()`](/Users/ivintik/dev/personal/codeharness/src/lib/workflow-parser.ts#L162) rejects that combination by design. Standalone legacy files may still parse, but legacy `extends: embedded://default` patches will regress.\n</issues>\n\nNo direct security issues stood out in the changed code.\n\nAC coverage is partial. AC1-6 and AC9-11 are reasonably covered by the template/parser tests. AC7 is only covered for standalone legacy files, not the patch-resolution path above. AC8 is only indirectly covered by “init copies the template”; there is no test proving an initialized project can actually run with the new format. That missing coverage is what let issue 1 slip through.",
  "acceptanceCriteria": [
    {
      "id": "AC1",
      "description": "**Given** the file `templates/workflows/default.yaml` after this change, **When** a user runs `codeharness validate templates/workflows/default.yaml`, **Then** the command exits 0 with no errors printed to stderr. <!-- verification: run `codeharness validate templates/workflows/default.yaml`, expect exit 0 -->",
      "status": "pending"
    },
    {
      "id": "AC2",
      "description": "**Given** the updated `templates/workflows/default.yaml`, **When** a user opens the file and inspects its contents, **Then** the file contains a `workflow:` key with a `for_each: epic` block at the top level — and does NOT contain `story_flow:`, `epic_flow:`, or `loop:` keys. <!-- verification: run `cat templates/workflows/default.yaml`, check output contains \"workflow:\" and \"for_each: epic\", and does NOT contain \"story_flow:\", \"epic_flow:\", or \"loop:\" -->",
      "status": "pending"
    },
    {
      "id": "AC3",
      "description": "**Given** the updated `templates/workflows/default.yaml`, **When** a user inspects the `workflow:` block, **Then** there is a nested `for_each: story` block inside `for_each: epic` containing story-level steps. <!-- verification: run `cat templates/workflows/default.yaml`, check output contains \"for_each: story\" nested under \"for_each: epic\" -->",
      "status": "pending"
    },
    {
      "id": "AC4",
      "description": "**Given** the updated `templates/workflows/default.yaml`, **When** a user inspects the story-level steps, **Then** they contain a `gate: quality` block with `check:` listing at least `check` and `review`, and `fix:` listing at least `retry`. <!-- verification: run `cat templates/workflows/default.yaml`, check output contains \"gate: quality\" with \"check:\" and \"fix:\" fields -->",
      "status": "pending"
    },
    {
      "id": "AC5",
      "description": "**Given** the updated `templates/workflows/default.yaml`, **When** a user inspects the epic-level steps (after `for_each: story`), **Then** there is a `gate: verification` block with `check:` listing `verify`. <!-- verification: run `cat templates/workflows/default.yaml`, check output contains \"gate: verification\" with \"check:\" including \"verify\" -->",
      "status": "pending"
    },
    {
      "id": "AC6",
      "description": "**Given** the updated `templates/workflows/default.yaml`, **When** a user counts the task definitions under `tasks:`, **Then** there are exactly 9 tasks: `create-story`, `implement`, `check`, `review`, `document`, `deploy`, `verify`, `retry`, `retro` — unchanged from before. <!-- verification: run `cat templates/workflows/default.yaml`, verify the `tasks:` section lists exactly 9 task names matching the expected list -->",
      "status": "pending"
    },
    {
      "id": "AC7",
      "description": "**Given** a YAML file using the old `story_flow` / `epic_flow` format (without `workflow:` key), **When** a user runs `codeharness validate <file>`, **Then** the command still exits 0 — backward compatibility is preserved for user-authored files that haven't migrated yet. <!-- verification: create a test YAML file with old-format keys (story_flow, epic_flow, loop), run `codeharness validate <file>`, expect exit 0 -->",
      "status": "pending"
    },
    {
      "id": "AC8",
      "description": "**Given** a new project is initialized via `codeharness init`, **When** the user inspects the generated workflow file, **Then** it uses the new `workflow:` / `for_each:` / `gate:` format (not the old `story_flow` / `epic_flow` format). <!-- verification: run `codeharness init` in a temp directory, inspect the generated workflow YAML, confirm it contains \"workflow:\" and \"for_each:\" and does not contain \"story_flow:\" or \"epic_flow:\" -->",
      "status": "pending"
    },
    {
      "id": "AC9",
      "description": "**Given** the project is built via `npm run build`, **When** the build completes, **Then** it exits 0 with no TypeScript errors. <!-- verification: `npm run build` exits 0 -->",
      "status": "pending"
    },
    {
      "id": "AC10",
      "description": "**Given** the full test suite is run via `npx vitest run`, **When** all tests complete, **Then** all existing tests pass — zero failures, no regressions. <!-- verification: `npx vitest run` exits 0 -->",
      "status": "pending"
    },
    {
      "id": "AC11",
      "description": "**Given** the updated template is validated by the parser, **When** the parser returns the resolved workflow, **Then** any test that previously referenced `templates/workflows/default.yaml` still passes — no test regressions from the format change. <!-- verification: `npx vitest run` exits 0 (same as AC10, confirms no test references to old format keys break) -->",
      "status": "pending"
    }
  ]
}
