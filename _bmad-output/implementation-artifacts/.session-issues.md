# Session Issues Log — 2026-03-24 (session 2)

### 10-2-nodejs-provider — create-story (2026-03-24T07:58Z)

- **Observation:** Existing `10-2-nodejs-provider.md` had minimal 3-AC version; overwrote with detailed 18-AC version with task breakdown and verification tags.
- **Observation:** `CoverageToolInfo` type mismatch between `stacks/types.ts` (`{ tool, configFile? }`) and `coverage.ts` (`{ tool, runCommand, reportFormat }`). Story notes this — reconciliation deferred to story 10-5.
- **Risk:** Epic AC3 says "remove all Node.js if/else branches from consumers" but incremental migration strategy defers this to 10-5. Replaced with AC18 (zero regressions) to match actual intent.
- **Problems:** None.
- **Workarounds:** None.

### 10-2-nodejs-provider — dev-story (2026-03-24T08:02Z)

- **OtlpResult type mismatch:** `stacks/types.ts` (`{ success, packagesInstalled, error? }`) differs from `otlp.ts` (`{ status, packages_installed, start_script_patched, env_vars_configured, error? }`). Provider uses stacks/types version. Reconciliation deferred to 10-5.
- **`patchStartScript` optional on interface but implemented.** Consumers need optional chaining `provider.patchStartScript?.(dir)`.
- **Pre-existing TS errors:** ~50 type errors in other test files (bridge, run, deps, etc.) unrelated to this story.
- **Consumer branches not removed** (by design per spec). Story 10-5 handles migration.
- **Test results:** 64/64 nodejs tests pass. Full suite: 3253 tests, 0 regressions.

### 10-2-nodejs-provider — code-review (2026-03-24T08:07Z)

- **HIGH (fixed):** `parseTestOutput()` vitest regex didn't capture skipped count — total was wrong.
- **HIGH (fixed):** `parseTestOutput()` jest regex failed when `skipped` appeared between `passed` and `total`. Rewrote to extract fields independently.
- **MEDIUM (fixed):** `utils.ts` at 70.58% coverage (below 80% floor) — `readTextSafe()` untested. Added utils.test.ts with 9 tests.
- **MEDIUM (fixed):** `nodejs.ts` catch branch in `patchStartScript` for malformed JSON untested. Added test.
- **LOW (not fixed):** `readTextSafe` catch branch (93.33%) — race condition between existsSync/readFileSync, not worth mocking.
- **LOW (not fixed):** `registry.ts` at 94.73% — lines from story 10-1, not in scope.
- **Coverage:** 97.09% overall, all 128 files above 80% floor. 3267 tests, 0 regressions.
