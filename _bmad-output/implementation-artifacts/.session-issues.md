# Session Issues Log — 2026-03-17

### 2-2-docker-compose-victoriametrics-stack-management — verification (2026-03-17T14:49Z)

- **Bug found:** `hasFailVerdict()` in `verify.ts` did not strip inline code backticks, causing `[FAIL]` references in proof narratives (e.g., "Reports `[FAIL] VictoriaMetrics stack: not running`") to be treated as failed ACs. Fixed by adding inline code stripping.
- **Bug found:** `checkBlackBoxEnforcement()` required every AC to have a `docker exec` command, but legitimate black-box evidence (e.g., `docker ps` for container status, `curl` to observability endpoints) was classified as `other` and rejected. Fixed by adding `docker-host` classification and accepting `observability` commands as valid evidence.
- **Issue:** Global `codeharness` binary was stale (npm install -g version) while local build had fixes. Had to run `npm install -g .` to sync. This is a recurring friction point during development.

### 2-3-observability-querying-agent-visibility-into-runtime — verification (2026-03-17T14:55Z)

- **Infrastructure failure:** Verifier session (`claude --print`) timed out after 10+ minutes without producing a proof document. The story has 7 ACs with substantial scope (plugin knowledge files, skills, hooks, observability endpoints). The verifier likely got stuck or ran out of budget.
- **Observation:** Running `claude --print` as a subprocess for verification is slow and unreliable. The 10-minute total timeout for the verification flow is tight when the verifier itself is a Claude session that needs to run multiple docker exec commands.
- **Retry state:** Set to 1/3. Next session will retry.
