# Session Issues Log — 2026-03-17

### 1-1-result-type-shared-types — create-story (2026-03-17T19:12Z)

**Problems encountered:**
- `_bmad/bmm/config.yaml` path referenced in workflow.yaml did not exist — found at `_bmad/config.yaml` instead.
- No `project-context.md` file exists in the repo.

**Workarounds applied:**
- Skipped sprint-status.yaml updates per caller instructions.
- Skipped web research on library versions — pure TypeScript types story with no external deps.

**Risks identified:**
- Naming collision between `src/lib/output.ts` (ok/fail as loggers) and `src/types/result.ts` (ok/fail as constructors) — needs careful aliasing in future stories.
- `ObservabilityBackend` query/result parameter types not specified in architecture doc — dev agent must design these.

**Observations:**
- First story in the overhaul. `src/types/` and `src/modules/` don't exist yet.
- All 6 ACs tagged cli-verifiable.

### 1-1-result-type-shared-types — dev-story (2026-03-17T19:18Z)

**Problems encountered:**
- BATS not installed locally — `npm test` fails for integration tests. Expected since BATS runs in CI only. Vitest unit tests all pass.

**Workarounds applied:**
- Verification/documentation patches in story (proof doc at `docs/exec-plans/`, AGENTS.md) not created — directories don't exist and these are boilerplate from codeharness patch system.

**Code quality concerns:**
- Coverage reporting on pure type files (`state.ts`, `observability.ts`, `index.ts`) shows 0% because they contain only TypeScript type definitions with zero runtime code. `result.ts` has 100%.

**Observations:**
- 21 new tests across 3 test files. All 1674 tests across 56 files pass.

### 1-1-result-type-shared-types — code-review (2026-03-17T19:25Z)

**Bugs found:**
- HIGH: Dead code in `fail()` constructor — allocated unused object when context provided. Fixed.
- HIGH: Dead code branch in `readPatchFile()` — duplicate path computation made second branch unreachable. Fixed.

**Code quality issues NOT fixed (LOW):**
- `TraceQuery` has all identifying fields optional, allowing unbounded trace queries. Future backends should add runtime validation.
- Type-only files show 0% coverage — correct behavior but could confuse reports.

**Coverage gaps:**
- No integration tests for cross-module type imports through barrel `index.ts`.

**Architecture concerns:**
- ok()/fail() naming collision between types/result.ts and lib/output.ts documented but no namespace prefix adopted.
