# Session Issues Log — 2026-03-25

### 15-3-template-migration-static-files — code-review (2026-03-25T02:35:00Z)

- **BLOCKER: Disk full (ENOSPC)** — /private/tmp filesystem has zero free space. The Bash tool writes output to /private/tmp/claude-501/ and cannot function. No tests, coverage, or verification commands can run.
- Code review passed with 0 HIGH/MEDIUM issues. 4 LOW issues identified (pre-existing, not from this story).
- Story status was already `verifying` when review started.
- LOW: orphaned template files at templates root (docker-compose.harness.yml, otel-collector-config.yaml, docker-compose.elk.yml) — cleanup candidates.
- LOW: otelCollectorConfigWithCors() uses fragile regex injection on base template output.
- LOW: dockerComposeTemplate() ignores config parameter.
- Could not run `codeharness coverage --min-file 80` due to disk full.

### Session-level — infrastructure (2026-03-25T02:40:00Z)

- **CRITICAL: Disk full prevents ALL Bash execution.** No verification, testing, building, or git operations possible. Session is effectively dead for implementation work. User must free disk space before next session.

### 15-3-template-migration-static-files — dev-story (2026-03-25T05:35:00Z)

- Implementation was already complete from previous session. Dev agent verified build + tests pass (31/31 template migration tests, all other test suites pass).
- `verify-prompt.ts` and `showboat-template.ts` NOT migrated — they contain complex programmatic logic (conditionals, loops, switch statements) incompatible with simple `{{VAR}}` interpolation. Left as TypeScript generators intentionally.
- Pre-existing TypeScript errors in unrelated test files (bridge, run, stack, status, sync, teardown) — not from this story.

### 15-3-template-migration-static-files — code-review (2026-03-25T05:45:00Z)

- PASS — no HIGH or MEDIUM issues found.
- LOW: Ralph prompt template uses camelCase `{{projectDir}}` while other templates use `{{SCREAMING_SNAKE}}`. Inconsistent but functional. Introduced by this story.
- LOW (pre-existing): dockerComposeTemplate() ignores `shared` parameter. otelCollectorConfigWithCors() uses fragile regex. bmad-patches.ts uses own path resolution.
- Coverage: 97.1% overall, all 156 files above 80% floor. 3777 tests pass.

### Infrastructure — Docker keychain (2026-03-25T05:35:00Z)

- Docker credential store (`osxkeychain`) is locked in this non-interactive session. All `docker pull` commands fail. Tried: direct pull, DOCKER_CONFIG env var, --config flag — all failed. Stories requiring Docker verification (14-5, 14-6) are blocked.
