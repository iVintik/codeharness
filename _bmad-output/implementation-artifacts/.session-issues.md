# Session Issues Log — 2026-04-03

### 5-3-crash-recovery-resume — create-story (2026-04-03T22:45)

- **Ambiguity in epic definition:** Epic AC says "no task executed twice" but doesn't specify mechanism. Resolved with tuple-based checkpoint matching `(task_name, story_key)`.
- **Ambiguity: loop block resume semantics.** Epic doesn't specify crash mid-loop-iteration behavior. Documented edge case explicitly.
- **AC #8 (memory/leak for 4+ hours):** Tagged test-provable — structural analysis approach rather than real 4-hour run. Borderline.
- **Missing `--resume` CLI flag:** Epic mentions `--resume` but that's story 5-4. This story is engine-level only.
- **No project-context.md found** in the repo.
- **PRD NFR numbering mismatch:** PRD uses different NFR numbers than architecture-v2.md.
- **Risk:** Loop block resume logic (Task 5) is most complex — iteration-increment guard needs careful implementation.
- **Risk:** AC #8 hard to prove definitively via unit tests.

## Token Report — create-story
- Total tool calls: 22
- By tool: Read: 10, Grep: 6, Bash: 2, Glob: 3, Write: 1
- Largest Bash outputs: `git diff --name-only` (~20 lines), `git log --oneline -5` (~5 lines)
- Files read: 10 unique, 10 total reads (no file read 3+ times)
- Redundant operations: None

### 5-3-crash-recovery-resume — dev-story (2026-04-03T22:51)

- **Architectural decision:** Story spec's `isTaskCompleted()` with (taskName, storyKey) tuple matching is insufficient for loop blocks — same pair recurs each iteration. Added `isLoopTaskCompleted()` that counts checkpoint occurrences vs iteration number. Deviation from spec but necessary for correctness.
- **Uncovered lines (97.32%):** Lines ~520, 554, 563-564 are edge cases in loop block iteration advancement for specific crash timing scenarios. Not worth contrived tests for marginal coverage gain.
- **No `--force` / `--fresh` flags:** Belong to story 5-4, not this story.

## Token Report — dev-story
- Total tool calls: 28
- By tool: Bash: 10, Read: 6, Edit: 14, Grep: 0, Write: 0, Glob: 4
- Largest Bash outputs: `npx vitest run` full suite (~30 lines), `npx vitest run workflow-engine` first failures (~40 lines), `find ... *.test.ts` (~20 lines)
- Files read: 5 unique, 8 total reads (workflow-engine.test.ts read 3 times)
- Redundant operations: vitest run 3 times (iterative development — all necessary)

### 5-3-crash-recovery-resume — code-review (2026-04-03T22:58)

- **HIGH bug found & fixed:** Error checkpoints treated as successful completions — `isTaskCompleted()` and `isLoopTaskCompleted()` didn't distinguish success vs error checkpoints, causing failed tasks to be permanently skipped on resume. Fixed by adding `error?: boolean` to `TaskCheckpoint`.
- **MEDIUM:** Test expectation mismatch exposed by HIGH fix — relied on buggy behavior. Fixed.
- **MEDIUM:** 3 missing branch coverage gaps addressed — added tests for loop block edge cases.
- **LOW (not fixed):** `projectDir ?? process.cwd()` fallback never tested — trivial.
- **LOW (not fixed):** `lastVerdict` null on resume causes conservative retry scope — safe but wasteful, tracked as tech debt.
- Coverage gaps: Lines 513-514, 563-564 still partially uncovered (unlikely edge cases).

## Token Report — code-review
- Total tool calls: 30
- By tool: Bash: 12, Read: 13, Edit: 9, Grep: 0, Write: 0, Glob: 1, Skill: 1
- Largest Bash outputs: coverage output (~80 lines), `npx vitest run` with failures (~30 lines), full suite (~20 lines)
- Files read: 7 unique, 16 total reads (workflow-engine.test.ts read 8 times — size limits)
- Redundant operations: 3 coverage runs needed for progressive verification; 8 partial reads of test file due to token limit

### 5-3-crash-recovery-resume — verify (2026-04-03T23:05)

- **BATS failures (253):** Pre-existing, missing shell scripts — not related to story 5-3.
- **Proof format mismatch:** First `codeharness verify` run failed (0/0 ACs) because proof format didn't match. Rewrote to match expected `## AC N:` pattern with `[PASS]` markers.
- **AC #8 weak evidence:** Memory usage bounded AC tested with proportional-growth assertion, not actual 4+ hour runtime profiling. Design-level verification only.

## Token Report — verify
- Total tool calls: 18
- By tool: Bash: 13, Read: 2, Grep: 3, Write: 2
- Largest Bash outputs: `npm test` (~80 lines), vitest coverage (~50 lines), vitest verbose grep (~30 lines)
- Files read: 2 unique, 2 total reads
- Redundant operations: Coverage ran twice (could have saved output from first), codeharness verify ran twice due to format mismatch
