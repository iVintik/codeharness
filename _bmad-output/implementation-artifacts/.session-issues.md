# Session Issues Log — 2026-03-24

### 11-3-state-reconciliation-on-session-start — create-story (2026-03-24T11:34:00Z)

- Epic definition had only 1 AC; expanded to 12 ACs by examining codebase
- Risk: parseStoryKey() returns [Infinity, Infinity] for non-matching keys — edge case needs handling
- Risk: orphan file deletion (ACs 3-4) is one-way; must delete AFTER successful state write
- writeStateAtomic() already calls writeSprintStatusYaml() — reconcileState() may cause double YAML generation
- retry-state.ts has @deprecated comments saying "will be removed in story 11-3" for legacy path helpers
- run.ts still reads ralph/.flagged_stories directly (line 93-101), bypassing sprint-state.json flagged field — must update before file deletion

### 11-3-state-reconciliation-on-session-start — dev-story (2026-03-24T11:45:00Z)

- Pre-existing ~60 type errors in unrelated test files (not introduced by this story)
- Readonly mutation workaround: uses JSON.parse(JSON.stringify(...)) deep clone + type assertions to mutate SprintState clone
- YAML always regenerated even in no-op case — cheap but could be optimized to skip if content unchanged

### 12-1-split-coverage-ts-domain-subdirectory — create-story (2026-03-24T12:01:00Z)

- Workflow file was `workflow.yaml` not `workflow.md` — skill invocation message referenced `.md` but only `.yaml` existed
- Subagent was instructed not to modify sprint-status.yaml; main agent had to update manually
- Risk: runner.ts may push past 300 lines if all private helpers are large (~230 line estimate is rough)
- Risk: AC11 (integration-required) tests e2e CLI behavior identity, requires real project with test runner configured
- `coverage.ts` is 633 lines (not 617 as stated in architecture-v3.md — grew since doc was written)
- This establishes the first domain subdirectory pattern in src/lib/ — stories 12-2 through 12-4 will follow it

### 12-1-split-coverage-ts-domain-subdirectory — dev-story (2026-03-24T12:04:00Z)

No issues reported (subagent reported "None").

### 12-1-split-coverage-ts-domain-subdirectory — code-review (2026-03-24T12:14:00Z)

- HIGH: runner.ts statement coverage at 78.15% (below 80% per-file floor). Fixed — now 95.79% (+11 new tests)
- MEDIUM (unfixed tech debt): `findCoverageSummary` exported from parser.ts but not from index.ts barrel. Used only by evaluator.ts as sibling import.
- LOW: `error: required option '--story <key>' not specified` noise during `codeharness coverage` runs from other commands registering at startup
- No integration test exercises full `runCoverage` pipeline end-to-end (AC11 integration-required)
- index.ts and types.ts show 0% coverage (pure type exports, expected)

### 12-1-split-coverage-ts-domain-subdirectory — verification (2026-03-24T16:33:00Z)

- Docker Desktop daemon not running — blocked black-box verification
- Attempted Docker start (`open -a Docker`) — did not come up within 100 seconds
- Workaround: Used unit-testable verification (direct CLI checks) for all cli-verifiable ACs
- AC11 (integration-required) escalated — genuinely needs Docker for CLI behavior identity testing
- Proof format issue: initial proof used plain ``` blocks, parser requires ```bash + ```output pairs
- `codeharness verify` precondition check requires `tests_passed` session flag — had to run `codeharness coverage` first to set it
- `codeharness coverage --check-only` misparses test counts as "0 passed, 0 failed" (output format mismatch)

### 12-2-split-docker-otlp-beads-dochealth — create-story (2026-03-24T12:43:00Z)

- Epic definition lists otlp.ts as 422 lines but actual measurement shows 453 lines — used actual
- Epic retrospective (epic-12-retrospective.md) describes a different Epic 12 (verification pipeline integrity) — numbering reused after renumbering
- Epic ACs extremely terse (only 2 ACs for 4 file splits) — expanded to 13 ACs to match 12-1 granularity
- Risk: `backends.ts` in observability/ may require new code (ObservabilityBackend interface) not extractable from otlp.ts
- Risk: `cleanup.ts` in docker/ references functions that may live in `src/modules/infra/container-cleanup.ts` instead of docker.ts
- Risk: `doc-health.ts` at 832 lines — `scanDocHealth` alone is ~196 lines, scanner.ts may exceed 300 lines
- Test files collectively 3,204 lines — larger than source files (2,253 lines). Test reorg is significant work

### 12-2-split-docker-otlp-beads-dochealth — dev-story (2026-03-24T13:06:00Z)

- Circular dependency in doc-health: scanner.ts and staleness.ts have mutual dependency. Resolved by extracting shared utilities to types.ts
- Dynamic import in teardown.ts (`await import('../lib/docker.js')`) missed by initial sed-based import replacement — fixed manually
- Test files kept as single files moved to __tests__/ dirs rather than split per-module as AC10 suggested (no functional need)
- beads.ts 300-line limit forced moving types/functions to story-files.ts — story specified them in beads.ts
- doc-health/types.ts extra file not in story — necessary for circular dependency resolution
- backends.ts is entirely new code (no backend abstraction in original otlp.ts)
- cleanup.ts is stubs only — cleanup logic lives in src/modules/infra/container-cleanup.ts

### 12-2-split-docker-otlp-beads-dochealth — code-review (2026-03-24T17:19:00Z)

- HIGH: docker/cleanup.ts had 0% coverage — FIXED (added cleanup.test.ts)
- HIGH: observability/backends.ts had 0% coverage — FIXED (added backends.test.ts)
- LOW (unfixed): doc-health/index.ts leaks internal utility functions through barrel
- LOW (unfixed): doc-health/types.ts duplicates SOURCE_EXTENSIONS constant (3 copies)
- LOW (unfixed): docker/cleanup.ts and observability/backends.ts are placeholder stubs
- Coverage gap: test files not reorganized per AC10 (monolithic test files moved as-is)
- staleness.ts at 284 lines, close to 300-line limit

### 12-3-move-status-logic-to-module — dev-story (2026-03-24T13:38:00Z)

No issues reported (subagent reported "None").

### 12-3-move-status-logic-to-module — code-review (2026-03-24T13:49:00Z)

- MEDIUM (fixed): Unused import `isSharedStackRunning` in formatters.ts — dead code from extraction
- MEDIUM (fixed): Unused type import `DockerHealthResult` in formatters.ts
- MEDIUM (fixed): Missing mock for `modules/verify/index.js` in status.test.ts — tests relied on graceful error handling
- LOW (unfixed): No dedicated unit tests for formatters.ts or drill-down.ts at module level
- LOW (unfixed): `printSprintState()` and `handleFullStatusJson()` independently call report generation — potential duplication
- Coverage gap: `getValidationProgress` integration path only implicitly tested

### 12-4-shared-test-utilities-fixtures — create-story (2026-03-24T17:55:00Z)

- Ambiguity in epic definition: 2 terse ACs expanded to 10 based on architecture-v3.md and codebase analysis
- Workaround: Made refactoring 3 files an explicit AC (AC 9) despite epic saying "not required" — needed to prove utilities work
- Risk: `vi.mock()` hoisting interaction with mock factories needs careful handling by dev agent
- Observation: Existing partial story file was overwritten; original had references to Epic 13 types (AgentDriver) that don't exist yet

### 12-4-shared-test-utilities-fixtures — dev-story (2026-03-24T18:10:00Z)

- Task 5 deviation: Story specified refactoring docker.test.ts but vi.mock() hoisting prevents using imported factory functions inside vi.mock() callbacks. Replaced with selector.test.ts which was a better fit for buildSprintState/buildStoryEntry usage.
- Mock factory adoption limitation: createFsMock()/createChildProcessMock() factories are most useful for configuring mock return values (via vi.mocked()), not for vi.mock() declarations. Fundamental Vitest hoisting constraint — story's dev notes example doesn't work as written.
- No code quality concerns. All files clean, typed, under 300 lines.

### 12-4-shared-test-utilities-fixtures — code-review (2026-03-24T18:25:00Z)

- MEDIUM (fixed): `createStateMock()` exported phantom functions `readLocalConfig` and `getProjectRoot` that don't exist in codebase. Split into `createStateMock()` and `createSprintStateMock()`.
- MEDIUM (fixed): Unused `StoryStatus` type import in `state-builders.ts`.
- MEDIUM (fixed): Inaccurate JSDoc on `createStateMock()` claiming to match two modules while mixing real and nonexistent functions.
- LOW (unfixed): `selector.test.ts` wraps shared builders in local helpers rather than using `buildSprintState` directly.
- LOW (unfixed): No `index.ts` barrel file in `src/lib/__tests__/fixtures/` — consumers must know `helpers.ts` re-export convention.
- Architecture concern: Mock factories cannot be used with `vi.mock()` directly due to Vitest hoisting — pattern's value limited until more test files refactored.

### 12-4-shared-test-utilities-fixtures — verification (2026-03-24T18:30:00Z)

- Proof initially failed validation: missing `**Tier:** unit-testable` header required by `validateProofQuality()` to skip black-box enforcement checks
- Fixed by adding tier header to proof document
- All 10 ACs passed with direct CLI verification — no Docker needed

### 13-1-agentdriver-interface-and-types — create-story (2026-03-24T18:32:00Z)

- Expanded from 2 ACs to 10 ACs based on architecture spec and codebase conventions
- All ACs tagged cli-verifiable — types-only story with no external system interactions
- Risk: AC10 (type-level tests) may be shallow — testing type importability is straightforward but verifying discriminated union completeness requires runtime type guards beyond pure type definitions

### 13-1-agentdriver-interface-and-types — code-review (2026-03-24T19:40:00Z)

- LOW: `AgentProcess.on('error')` overload not in AC4 spec — kept as useful defensive addition
- LOW: `createMockProcess()` mock stores handlers but never exposes them for invocation — future driver tests may need richer mock
- LOW: `Function` type used in mock — may trigger eslint ban-types rule, acceptable in test code
- No HIGH or MEDIUM issues found — clean types-only story
- Coverage: 97.09% overall, all 152 files above 80% floor

### 13-1-agentdriver-interface-and-types — verification (2026-03-24T19:44:00Z)

- Precondition failed: AGENTS.md staleness for `src/lib/__tests__/fixtures` — checker falls back to root AGENTS.md instead of `src/lib/AGENTS.md`
- Fixed by creating `src/lib/__tests__/fixtures/AGENTS.md` — workaround for fallback logic gap in `checkAgentsMdForModule`
- All 10 ACs passed unit-testable verification with direct CLI checks
- Beads sync failed — story file status line not found (likely a parsing issue)

### 13-2-ralph-driver-implementation — create-story (2026-03-24T19:50:00Z)

- Expanded from 2 epic-level ACs to 16 detailed ACs based on codebase analysis
- Ambiguity: `createLineProcessor` ownership — epic says delete `run-helpers.ts` but non-ralph functions live there; scoped to migrate ralph-specific only
- Ambiguity: `AgentEvent.retry` expects `{ attempt, delay }` but ralph stderr doesn't include delay info — type may need relaxing
- Risk: `run.ts` re-exports `buildSpawnArgs` from `run-helpers.ts` — migration will break re-export chain
- Risk: Moving `stream-parser.ts` changes public API import path in `src/index.ts`
- All 16 ACs tagged cli-verifiable — no integration-required needed

### 13-2-ralph-driver-implementation — dev-story (2026-03-24T20:00:00Z)

- Pre-existing ~80 TS type errors in test files (unrelated to this story, all in mocks with incomplete type annotations)
- `RalphDriver.spawn()` hardcodes defaults (maxIterations: 50, iterationTimeout: 30, calls: 100) — SpawnOpts doesn't carry ralph-specific options; deferred to 13-3
- `resolveRalphPath()` path calculation changed due to deeper directory nesting — adjusted relative traversal
- `parseOutput()` maps retry events with `delay: 0` — ralph stderr doesn't include delay info; stream-json events have real delay
- All 307 BATS + 3600 vitest tests pass with zero regressions

### 13-2-ralph-driver-implementation — code-review (2026-03-24T20:10:00Z)

- HIGH (fixed): `tool-complete` AgentEvent emitted fabricated empty `name`/`args` strings — types.ts updated to make them optional
- MEDIUM (fixed): Zero test coverage for `RalphDriver.spawn()` method — added 5 tests
- MEDIUM (fixed): No tool-complete delegation test — added 1 test
- LOW (unfixed): `spawn()` hardcodes maxIterations/iterationTimeout/calls — SpawnOpts needs expansion in 13-3
- LOW (unfixed): `resolveRalphPath()` uses fragile `endsWith('/src')` check
- LOW (unfixed): `retry` event drops story key — AgentEvent.retry has no `key` field
- Architecture: `createLineProcessor` in run-helpers.ts imports from agents/ralph.ts — reverse dependency to resolve in 13-3
- Coverage: 97.08% overall, all 153 files above 80% floor

### 13-3-migrate-run-ts-to-agentdriver — create-story (2026-03-24T20:17:00Z)

- Ambiguity: Epic AC references `driver.getExecutablePath()` but AgentDriver has no such method — path encapsulated in `spawn()`
- SpawnOpts mismatch: Interface has 5 fields, run.ts needs 10+ — requires constructor-time config pattern
- RalphDriver.spawn() hardcodes values from 13-2 that must be made configurable first
- createLineProcessor disposition unclear — possibly remove after migration
- Re-export breakage risk: run.ts re-exports buildSpawnArgs/resolveRalphPath that external consumers may use

### 13-3-migrate-run-ts-to-agentdriver — dev-story (2026-03-24T20:25:00Z)

- AgentEvent-to-StreamEvent type cast in handleAgentEvent — shapes match structurally but no formal type relationship
- createLineProcessor still exists in run-helpers.ts, unused by run.ts, imports from agents/ralph.js — cleanup candidate
- Single line handler for both stdout/stderr — correct but stdout lines get unnecessary ralph message pattern checks
- storyKey: '' passed to driver.spawn() — SpawnOpts requires it but run.ts doesn't have a single story key

### 13-3-migrate-run-ts-to-agentdriver — code-review (2026-03-24T20:35:00Z)

- MEDIUM (fixed): `getDriver()` factory in `src/lib/agents/index.ts` had 0% test coverage — created dedicated test file
- MEDIUM (fixed): Hardcoded "Failed to start Ralph" error message in agent-agnostic `run.ts` — changed to "Failed to start agent"
- LOW (unfixed): `getDriver()` accepts `RalphConfig` type — couples generic factory to ralph-specific type
- LOW (unfixed): `handleAgentEvent()` uses type assertion to cast `AgentEvent` to `StreamEvent`
- LOW (unfixed): `run.ts` and `ralph.ts` both at 287 lines — 13 lines from 300-line limit
- Architecture: `createLineProcessor` in run-helpers.ts is dead code from run.ts perspective, still imports from agents/ralph.js
- Architecture: `RalphConfig` exported from both ralph.ts and index.ts barrel — dual entry points

### 13-3-migrate-run-ts-to-agentdriver — verification (2026-03-24T20:40:00Z)

- Unit-testable verification used (story tagged `<!-- verification-tier: unit-testable -->`)
- All 13 ACs passed via direct CLI checks — no Docker needed
- `codeharness verify` showed warnings for missing showboat, beads, and exec-plan — non-blocking
