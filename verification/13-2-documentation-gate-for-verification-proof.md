# Verification Proof: 13-2-documentation-gate-for-verification

*2026-03-16T15:11:49Z by Showboat 0.6.1*
<!-- showboat-id: c79e6f04-ff74-40a0-a5fa-beace48753ad -->

## Story: 13-2 Documentation Gate for Verification

Acceptance Criteria:
1. verify fails with exit 1 and specific error when README.md missing
2. init scaffolds README.md with project name, install command, usage, CLI reference
3. Generated README has Quick Start with install, first-run, example
4. verify-env check validates CLI works inside Docker container
5. verify-env check fails with clear error when CLI broken in container

## Baseline: All tests pass

```bash
npm run test:unit 2>&1 | grep -E 'Test Files|Tests ' | head -2
```

```output
[2m Test Files [22m [1m[32m49 passed[39m[22m[90m (49)[39m
[2m      Tests [22m [1m[32m1559 passed[39m[22m[90m (1559)[39m
```

## AC1: README.md gate in verify command

verify --story must fail with exit 1 and error message when README.md is missing.

```bash
TMPDIR=$(mktemp -d) && cd "$TMPDIR" && echo '{"version":"1.0.0"}' > package.json && codeharness verify --story test-key 2>&1; echo "EXIT=$?"
```

```output
[FAIL] No README.md found — verification requires user documentation
EXIT=1
```

```bash
TMPDIR=$(mktemp -d) && cd "$TMPDIR" && echo '{"version":"1.0.0"}' > package.json && codeharness --json verify --story test-key 2>&1; echo "EXIT=$?"
```

```output
{"status":"fail","message":"No README.md found — verification requires user documentation"}
EXIT=1
```

AC1 PASS: verify exits 1 with exact error message in both text and JSON modes.

## AC2: README.md generation in codeharness init

init must scaffold README.md with project name, install command, usage, CLI reference. Verified via template source and unit tests.

```bash
grep -c 'Quick Start\|Installation\|Usage\|CLI Reference\|projectName\|installCommand' /Users/ivintik/dev/personal/codeharness/src/templates/readme.ts
```

```output
13
```

```bash
grep -n 'Quick Start\|## Installation\|## Usage\|## CLI Reference' /Users/ivintik/dev/personal/codeharness/src/templates/readme.ts
```

```output
32:    '## Quick Start',
45:    '## Installation',
51:    '## Usage',
61:    '## CLI Reference',
```

```bash
grep -n 'npm install -g codeharness\|pip install codeharness\|codeharness init\|codeharness status\|cliHelpOutput' /Users/ivintik/dev/personal/codeharness/src/templates/readme.ts
```

```output
6: * Generated by `codeharness init` when README.md does not exist.
16:  cliHelpOutput: string;
23:  const { projectName, stack, cliHelpOutput } = config;
26:  const firstRunCommand = 'codeharness init';
27:  const exampleUsage = 'codeharness status';
56:    'codeharness init',
64:    cliHelpOutput.trimEnd(),
74:    return 'pip install codeharness';
76:  return 'npm install -g codeharness';
```

```bash
grep -n 'README\|readme' /Users/ivintik/dev/personal/codeharness/src/commands/init.ts | head -15
```

```output
15:import { readmeTemplate } from '../templates/readme.js';
47:    readme: 'created' | 'exists';
203:          readme: 'created',
219:            result.documentation.readme = 'exists';
458:      // --- README.md generation ---
459:      const readmePath = join(projectDir, 'README.md');
460:      if (!existsSync(readmePath)) {
472:        const readmeContent = readmeTemplate({
477:        generateFile(readmePath, readmeContent);
478:        result.documentation.readme = 'created';
480:        result.documentation.readme = 'exists';
487:        if (result.documentation.readme === 'created') {
488:          ok('Documentation: README.md created');
```

```bash
npx vitest run src/lib/__tests__/readme.test.ts 2>&1 | grep -E 'Test Files|Tests ' | head -2
```

```output
[2m Test Files [22m [1m[32m1 passed[39m[22m[90m (1)[39m
[2m      Tests [22m [1m[32m10 passed[39m[22m[90m (10)[39m
```

```bash
npx vitest run src/commands/__tests__/init.test.ts 2>&1 | grep -E 'Test Files|Tests ' | head -2
```

```output
[2m Test Files [22m [1m[32m1 passed[39m[22m[90m (1)[39m
[2m      Tests [22m [1m[32m107 passed[39m[22m[90m (107)[39m
```

AC2 PASS: Template generates all required sections. init.ts calls readmeTemplate() and writes README.md when missing, preserves existing. Unit tests (10 template + 107 init) all pass.

## AC3: Quick Start section

Quick Start includes install command, first-run (codeharness init), example (codeharness status). Verified by template source lines 26-27, 32-43 and unit tests.

```bash
grep -A 2 'Quick Start\|firstRunCommand\|exampleUsage' /Users/ivintik/dev/personal/codeharness/src/templates/readme.ts | head -15
```

```output
  const firstRunCommand = 'codeharness init';
  const exampleUsage = 'codeharness status';

  const lines: string[] = [
--
    '## Quick Start',
    '',
    '```bash',
--
    firstRunCommand,
    '',
    '# Check project status',
    exampleUsage,
    '```',
    '',
```

AC3 PASS: Quick Start section contains install command, codeharness init, codeharness status.

## AC4+AC5: verify-env check CLI validation

AC4: verify-env check runs codeharness --version inside Docker to validate CLI works.
AC5: When CLI fails, outputs clear build/packaging error message.
Both require Docker with verification image. Verified via unit tests and source inspection.

```bash
sed -n '340,349p' /Users/ivintik/dev/personal/codeharness/src/lib/verify-env.ts
```

```output
  // 2. Check CLI works inside container
  try {
    execFileSync('docker', ['run', '--rm', IMAGE_TAG, 'codeharness', '--version'], {
      stdio: 'pipe',
      timeout: 30_000,
    });
    result.cliWorks = true;
  } catch {
    result.cliWorks = false;
  }
```

```bash
grep -n 'CLI does not work' /Users/ivintik/dev/personal/codeharness/src/commands/verify-env.ts
```

```output
100:          jsonResult.message = 'CLI does not work inside verification container — build or packaging is broken';
116:            fail('CLI does not work inside verification container — build or packaging is broken');
```

```bash
npx vitest run src/commands/__tests__/verify-env.test.ts 2>&1 | grep -E 'Test Files|Tests ' | head -2
```

```output
[2m Test Files [22m [1m[32m1 passed[39m[22m[90m (1)[39m
[2m      Tests [22m [1m[32m18 passed[39m[22m[90m (18)[39m
```

```bash
npx vitest run src/commands/__tests__/verify.test.ts 2>&1 | grep -E 'Test Files|Tests ' | head -2
```

```output
[2m Test Files [22m [1m[32m1 passed[39m[22m[90m (1)[39m
[2m      Tests [22m [1m[32m34 passed[39m[22m[90m (34)[39m
```

AC4 PASS: verify-env check runs docker run --rm IMAGE codeharness --version and sets cliWorks=true on exit 0. 18 unit tests pass.

AC5 PASS: When imageExists && !cliWorks, outputs 'CLI does not work inside verification container -- build or packaging is broken' in both text and JSON modes. Covered by unit tests.

## Final test run

```bash
npm run test:unit 2>&1 | grep -E 'Test Files|Tests ' | head -2
```

```output
[2m Test Files [22m [1m[32m49 passed[39m[22m[90m (49)[39m
[2m      Tests [22m [1m[32m1559 passed[39m[22m[90m (1559)[39m
```

## Verdict: PASS

- Total ACs: 5
- Verified: 5
- Failed: 0
- Tests: 1559 passing (49 files)
- Showboat verify: reproducible

AC1: CLI verify exits 1 with exact error when README.md missing (text + JSON modes)
AC2: Template generates README with all required sections; init.ts integrates it
AC3: Quick Start includes install, first-run, example usage
AC4: verify-env check validates CLI inside Docker via codeharness --version
AC5: Broken CLI produces clear build/packaging error in text and JSON modes

Note: AC4/AC5 verified via unit tests and source inspection (integration testing requires Docker image build). AC2 init integration verified via unit tests (live init requires beads server).
