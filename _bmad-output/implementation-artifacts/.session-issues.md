# Session Issues Log — 2026-03-18

### 5-1-review-module-extraction — create-story (2026-03-18T19:08Z)

**Problems encountered:** None.

**Workarounds applied:**
- Epic definition has only 4 ACs, which are too coarse for implementation. Expanded to 8 ACs covering timeout handling, file size limits, type completeness, and orchestrator details.

**Risks identified:**
- AC #3 (review returns to in-progress, triggers dev with findings) depends on the sprint loop interpreting `ReviewResult.approved === false` and transitioning story status. Sprint loop may not have this logic yet.
- `approved` field parsing is ambiguous — `claude --print` output from code-review workflow has no standardized machine-readable approval/rejection signal. Orchestrator will need a heuristic.

**Observations:**
- Existing review module is a pure stub (`fail('not implemented')`). `ReviewResult` type missing `duration` and `output` fields.
- Dev module orchestrator at `src/modules/dev/orchestrator.ts` is a clean 177-line reference implementation.

### 5-1-review-module-extraction — dev-story (2026-03-18T19:12Z)

- **`npm test` uses BATS** which isn't installed locally, so full integration test suite couldn't run. Verified via `npx vitest run` (2007/2007 pass).
- **Review output parsing is heuristic-based** — `parseReviewOutput` matches keywords like "changes requested", "not approved", "LGTM". Fragile since Claude's output format isn't guaranteed.
- **Code duplication with dev orchestrator** — `truncateOutput`, `captureFilesChanged`, `isTimeoutError`, and git diff logic copy-pasted from `src/modules/dev/orchestrator.ts`. Should be extracted to shared utility in future.
- **No `filesChanged` on ReviewResult** — dev module has it but story spec doesn't require it. Correct per spec since review shouldn't modify files.

### 5-1-review-module-extraction — code-review (2026-03-18T19:16Z)

**Bugs found and fixed:**
- HIGH: Dead code — `captureFilesChanged()` and `execSync` import copied from dev module but never wired up. Removed.
- HIGH: Boolean logic bug — `parseReviewOutput` had tautological expression. Simplified.
- MEDIUM: Test mocking stale references to removed dead code. Cleaned up.

**Code quality issues (LOW, not fixed):**
- `parseReviewOutput` uses simple substring matching — fragile against LLM output variations.
- `isTimeoutError` checks `signal === 'SIGTERM'` which could misclassify non-timeout kills.
- Code duplication between dev and review orchestrators (~60% identical). Needs consolidation.

**Coverage gaps:**
- No integration test for AC #3 (review rejection triggering story status transition back to `in-progress`).

### 3-1-error-capture-on-timeout — verification (2026-03-18T19:30Z)

**Problems encountered:**
- Container missing codeharness CLI — `verify-env build` doesn't pre-install the artifact. Had to `npm install -g codeharness` inside container.
- AC 6 not implemented by dev agent — dev explicitly skipped it, calling it "beyond scope." Verification correctly caught the gap.

**Workarounds applied:**
- Manual npm install in container for verification.

**Risks identified:**
- verify-env build should install the project CLI into the container. This will affect all future verifications.

**Observations:**
- 5/6 ACs pass. AC 6 (status --story drill-down showing timeout info) was not implemented. Returning to dev.

### 3-1-error-capture-on-timeout — dev-story (2026-03-18T19:35Z)

- **Code quality concern:** `findLatestTimeoutReport` parses markdown with regex to extract duration/files-changed — fragile if report format changes. Structured metadata (JSON sidecar) would be more robust.
- **Observation:** `status.ts` is 722 lines, well above NFR18 300-line limit. Predates this change but needs splitting.
- **Observation:** `drill-down.ts` header says "no filesystem access" — timeout data flows through orchestration layer in `index.ts`, keeping the pure function pure.

### 3-1-error-capture-on-timeout — code-review (2026-03-18T19:40Z)

**Bugs found and fixed:**
- HIGH: No test coverage for timeout summary display in `status --story` command (text and JSON). Added 4 tests.
- MEDIUM: `drill-down.ts` opts parameter untested. Added 3 tests.
- MEDIUM: AGENTS.md stale — `findLatestTimeoutReport`, `TimeoutSummary`, and `opts?` undocumented. Fixed.
- MEDIUM: Pre-existing mock data missing `timeoutSummary` field.

**Code quality issues (LOW, not fixed):**
- `index.test.ts` doesn't verify `findLatestTimeoutReport` integration in orchestration.
- `status.ts` at 722 lines exceeds NFR18 300-line limit (pre-existing).

**Coverage:** 96.46% overall, all 77 files above 80%.

**Session time constraint:** Story left at `verifying` — not enough time for full Docker verification this session. Next session will pick it up as Tier C.
