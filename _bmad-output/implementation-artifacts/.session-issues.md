# Session Issues Log — 2026-03-23 (session 6)

### 9-1-multi-stack-detection-subdir-scanning — create-story (2026-03-23T14:05Z)

None.

### 9-1-multi-stack-detection-subdir-scanning — dev-story (2026-03-23T14:08Z)

- **Observation:** Story technical notes suggested `vi.mock('node:fs')` but existing tests use real temp directories. Followed existing pattern for consistency.
- **Observation:** AC2 specifies return order `[nodejs/frontend, rust/backend]` but AC6 says alphabetical by dir name — which gives `[backend, frontend]`. Implementation follows AC6. Spec inconsistency in story file.

### 9-1-multi-stack-detection-subdir-scanning — code-review (2026-03-23T14:12Z)

- **HIGH (fixed):** `detectStack()` compat wrapper returned subdirectory stacks — broke root-only callers. Fixed to filter `.dir === '.'`.
- **HIGH (fixed):** SKIP_DIRS missing `.venv`, `venv`, `.tox`, `.mypy_cache`, `.cache` — false-positive Python detection from venvs.
- **MEDIUM (fixed):** `stack` field typed as `string` instead of `StackName` union — lost type safety.
- **MEDIUM (fixed):** Unused type import in test file.
- **LOW (not fixed):** `readdirSync` follows symlinks, no test for non-existent dir catch branch, `detectStack()` return type not narrowed to `StackName | null` (would require downstream changes).
- **Divergence from tech spec:** Tech spec says `detectStack()` returns `stacks[0]?.stack`, but review fix changed to filter root-only. Downstream stories 9-2 through 9-5 need to be aware.
