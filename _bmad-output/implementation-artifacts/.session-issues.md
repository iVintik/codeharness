# Session Issues Log — 2026-03-24

### 11-3-state-reconciliation-on-session-start — create-story (2026-03-24T11:34:00Z)

- Epic definition had only 1 AC; expanded to 12 ACs by examining codebase
- Risk: parseStoryKey() returns [Infinity, Infinity] for non-matching keys — edge case needs handling
- Risk: orphan file deletion (ACs 3-4) is one-way; must delete AFTER successful state write
- writeStateAtomic() already calls writeSprintStatusYaml() — reconcileState() may cause double YAML generation
- retry-state.ts has @deprecated comments saying "will be removed in story 11-3" for legacy path helpers
- run.ts still reads ralph/.flagged_stories directly (line 93-101), bypassing sprint-state.json flagged field — must update before file deletion

### 11-3-state-reconciliation-on-session-start — dev-story (2026-03-24T11:45:00Z)

- Pre-existing ~60 type errors in unrelated test files (not introduced by this story)
- Readonly mutation workaround: uses JSON.parse(JSON.stringify(...)) deep clone + type assertions to mutate SprintState clone
- YAML always regenerated even in no-op case — cheap but could be optimized to skip if content unchanged

### 12-1-split-coverage-ts-domain-subdirectory — create-story (2026-03-24T12:01:00Z)

- Workflow file was `workflow.yaml` not `workflow.md` — skill invocation message referenced `.md` but only `.yaml` existed
- Subagent was instructed not to modify sprint-status.yaml; main agent had to update manually
- Risk: runner.ts may push past 300 lines if all private helpers are large (~230 line estimate is rough)
- Risk: AC11 (integration-required) tests e2e CLI behavior identity, requires real project with test runner configured
- `coverage.ts` is 633 lines (not 617 as stated in architecture-v3.md — grew since doc was written)
- This establishes the first domain subdirectory pattern in src/lib/ — stories 12-2 through 12-4 will follow it

### 12-1-split-coverage-ts-domain-subdirectory — dev-story (2026-03-24T12:04:00Z)

No issues reported (subagent reported "None").

### 12-1-split-coverage-ts-domain-subdirectory — code-review (2026-03-24T12:14:00Z)

- HIGH: runner.ts statement coverage at 78.15% (below 80% per-file floor). Fixed — now 95.79% (+11 new tests)
- MEDIUM (unfixed tech debt): `findCoverageSummary` exported from parser.ts but not from index.ts barrel. Used only by evaluator.ts as sibling import.
- LOW: `error: required option '--story <key>' not specified` noise during `codeharness coverage` runs from other commands registering at startup
- No integration test exercises full `runCoverage` pipeline end-to-end (AC11 integration-required)
- index.ts and types.ts show 0% coverage (pure type exports, expected)

### 12-1-split-coverage-ts-domain-subdirectory — verification (2026-03-24T16:33:00Z)

- Docker Desktop daemon not running — blocked black-box verification
- Attempted Docker start (`open -a Docker`) — did not come up within 100 seconds
- Workaround: Used unit-testable verification (direct CLI checks) for all cli-verifiable ACs
- AC11 (integration-required) escalated — genuinely needs Docker for CLI behavior identity testing
- Proof format issue: initial proof used plain ``` blocks, parser requires ```bash + ```output pairs
- `codeharness verify` precondition check requires `tests_passed` session flag — had to run `codeharness coverage` first to set it
- `codeharness coverage --check-only` misparses test counts as "0 passed, 0 failed" (output format mismatch)

### 12-2-split-docker-otlp-beads-dochealth — create-story (2026-03-24T12:43:00Z)

- Epic definition lists otlp.ts as 422 lines but actual measurement shows 453 lines — used actual
- Epic retrospective (epic-12-retrospective.md) describes a different Epic 12 (verification pipeline integrity) — numbering reused after renumbering
- Epic ACs extremely terse (only 2 ACs for 4 file splits) — expanded to 13 ACs to match 12-1 granularity
- Risk: `backends.ts` in observability/ may require new code (ObservabilityBackend interface) not extractable from otlp.ts
- Risk: `cleanup.ts` in docker/ references functions that may live in `src/modules/infra/container-cleanup.ts` instead of docker.ts
- Risk: `doc-health.ts` at 832 lines — `scanDocHealth` alone is ~196 lines, scanner.ts may exceed 300 lines
- Test files collectively 3,204 lines — larger than source files (2,253 lines). Test reorg is significant work

### 12-2-split-docker-otlp-beads-dochealth — dev-story (2026-03-24T13:06:00Z)

- Circular dependency in doc-health: scanner.ts and staleness.ts have mutual dependency. Resolved by extracting shared utilities to types.ts
- Dynamic import in teardown.ts (`await import('../lib/docker.js')`) missed by initial sed-based import replacement — fixed manually
- Test files kept as single files moved to __tests__/ dirs rather than split per-module as AC10 suggested (no functional need)
- beads.ts 300-line limit forced moving types/functions to story-files.ts — story specified them in beads.ts
- doc-health/types.ts extra file not in story — necessary for circular dependency resolution
- backends.ts is entirely new code (no backend abstraction in original otlp.ts)
- cleanup.ts is stubs only — cleanup logic lives in src/modules/infra/container-cleanup.ts

### 12-2-split-docker-otlp-beads-dochealth — code-review (2026-03-24T17:19:00Z)

- HIGH: docker/cleanup.ts had 0% coverage — FIXED (added cleanup.test.ts)
- HIGH: observability/backends.ts had 0% coverage — FIXED (added backends.test.ts)
- LOW (unfixed): doc-health/index.ts leaks internal utility functions through barrel
- LOW (unfixed): doc-health/types.ts duplicates SOURCE_EXTENSIONS constant (3 copies)
- LOW (unfixed): docker/cleanup.ts and observability/backends.ts are placeholder stubs
- Coverage gap: test files not reorganized per AC10 (monolithic test files moved as-is)
- staleness.ts at 284 lines, close to 300-line limit
