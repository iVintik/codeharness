# Session Issues Log — 2026-03-19 (session 10)

### 1-2-analyzer-module-interface — create-story (2026-03-19T07:42Z)

- **Ambiguity in coverage computation (AC #5):** Epic AC says "20 functions and 15 with log statements" but Semgrep doesn't directly report total function count — it reports functions WITHOUT logs. Workaround: derive `totalFunctions = functionsWithLogs + matchCount` from the `function-no-debug-log` rule.
- **Risk — Semgrep JSON output format:** Story assumes specific Semgrep JSON output structure. No version pinning.
- **Observation:** AnalyzerResult/ObservabilityGap types should live in `src/modules/observability/types.ts` per module boundary convention, not shared `src/types/`.

### 1-2-analyzer-module-interface — dev-story (2026-03-19T07:50Z)

- **Coverage computation design gap (Task 3.3):** Semgrep `--json` output doesn't provide total function count. Added optional `totalFunctions` parameter to `computeSummary()` so callers can provide count from external source. Without override, defaults to match count (pessimistic).
- **`computeSummary` signature differs from spec:** Changed from `computeSummary(gaps, raw)` to `computeSummary(gaps, opts?)` because raw output doesn't contain needed stats. The `_raw` parameter would have been unused dead code.
- **`index.ts` and `types.ts` show 0% coverage** — expected, they contain only type exports and re-exports with no runtime code.

### 1-2-analyzer-module-interface — code-review (2026-03-19T07:55Z)

- **HIGH — `runSemgrep` no JSON validation:** Blindly cast `JSON.parse` output to `SemgrepRawOutput` without validating `results` array exists. Malformed output would cause runtime TypeError. Fixed.
- **HIGH — `parseSemgrepOutput` crash on missing results:** Would throw TypeError if called with `SemgrepRawOutput` where `results` is missing/not array. Fixed with defensive guard.
- **MEDIUM — `index.ts` barrel had 0% coverage:** No test imported from barrel. Fixed by adding barrel import test.
- **MEDIUM — No tests for malformed Semgrep JSON:** Added 5 new test cases covering edge cases.
- **LOW (unfixed) — `normalizeSeverity`:** Silently maps unrecognized severity strings to `info`. No logging/warning.
- **LOW (unfixed) — `computeSummary` rounding:** Rounds coverage to 2 decimal places via `Math.round(x * 100) / 100` — precision behavior undocumented.

### 1-2-analyzer-module-interface — verify (2026-03-19T08:00Z)

- **Infra: Tree-shaking removed analyzer module from dist/.** tsup only had `src/index.ts` as entry point. Analyzer module not referenced from CLI → bundler eliminated it. Fixed by adding `src/modules/observability/index.ts` as separate tsup entry point in `dist/modules/observability/`.
- **Bug: AC5 FAIL — Rule ID mismatch in computeSummary().** Compares `g.type === "function-no-debug-log"` but Semgrep produces path-prefixed IDs like `"tmp.test-ac5c.patches.observability.function-no-debug-log"`. Needs endsWith() or includes() instead of strict equality.
- **Bug: AC5 FAIL — totalFunctions derivation broken.** Defaults to functionsWithoutLogs count (0 due to rule ID bug). Even fixed, would report wrong coverage because totalFunctions must come from somewhere (not directly available from Semgrep).
- **4/5 ACs passed.** Story returned to in-progress for AC5 fix.

### 1-2-analyzer-module-interface — dev-story fix cycle (2026-03-19T08:10Z)

- **Fixed rule ID mismatch:** Added `matchesRule()` helper using `endsWith()` for path-prefixed Semgrep check_ids.
- **Same fix applied to all rule matchers:** `catch-without-logging` and `error-path-no-log` also use `endsWith()` now.
- **totalFunctions still caller-provided:** No automatic function counting — callers must pass `opts.totalFunctions`. Fallback is pessimistic (coverage = 0%).
- **`matchesRule` not independently tested:** Private helper tested only through `computeSummary`. Acceptable for now.

### 1-2-analyzer-module-interface — code-review fix cycle (2026-03-19T08:15Z)

- **HIGH — `analyze()` never passed `totalFunctions` to `computeSummary()`:** Coverage always 0% in real usage. Fixed by adding `totalFunctions` to `AnalyzerConfig` and wiring through.
- **MEDIUM — Empty `projectDir` accepted:** Would spawn Semgrep with empty path. Fixed with input validation.
- **LOW (unfixed) — `computeSummary()` can produce negative `functionsWithLogs`:** No clamping when `totalFunctions < functionsWithoutLogs`.
- **LOW (unfixed) — `levelDistribution` field name misleading:** Tracks gap severity, not log levels.
