# Session Issues Log — 2026-04-03

### 5-4-run-status-commands — create-story (2026-04-03T03:15)

- **Config path mismatch:** `_bmad/bmm/config.yaml` doesn't exist; actual config at `_bmad/config.yaml`.
- **Sprint-status.yaml stale:** Epic 5 marked `backlog` despite 3/4 stories done.
- **Risk:** `--resume` flag semantics ambiguous — engine already resumes by default (story 5-3). Could conflict with "completed run early exit" behavior.
- **Risk:** UX spec shows richer `status` output than epic ACs cover. Story follows epic scope.
- **Observation:** Last story in Epic 5 — purely CLI wiring, no new engine logic.

## Token Report — create-story
- Total tool calls: 21
- By tool: Bash: 2, Read: 14, Glob: 6, Write: 1, Grep: 1, Skill: 1
- Largest Bash outputs: `git log --oneline -10` (~10 lines), `ls status/` (~6 lines)
- Files read: 14 unique, 15 total reads (no file read 3+ times)
- Redundant operations: one redundant read of sprint-status.yaml

### 5-4-run-status-commands — dev-story (2026-04-03T03:22)

No issues reported by dev agent.

## Token Report — dev-story
- Total tool calls: 24
- By tool: Bash: 8, Read: 10, Edit: 7, Grep: 2, Write: 2, Glob: 1
- Largest Bash outputs: `npx vitest run` full suite (~30 lines), targeted tests (~28 lines), `ls` commands (~26 lines)
- Files read: 12 unique, 12 total reads (no file read 3+ times)
- Redundant operations: none

### 5-4-run-status-commands — code-review (2026-04-03T03:28)

- **HIGH bug found & fixed:** `--resume` flag registered but never read — `options.resume` ignored, engine's `phase === 'completed'` early-exit was never bypassed.
- **MEDIUM:** `formatElapsed` duplicated in 3 files with 3 different implementations. Not fixed — cross-cutting refactor outside story scope.
- **MEDIUM:** 5 CLI options (`--timeout`, `--iteration-timeout`, `--calls`, `--max-story-retries`, `--reset`) parsed but never passed to EngineConfig. Pre-existing tech debt.
- **MEDIUM:** Story File List incomplete (missing `run-helpers.ts` dependency). Fixed.
- **LOW (not fixed):** `run.ts` at 172 lines exceeds aspirational "under 100" guidance but within NFR9 300-line limit.
- **LOW (not fixed):** `formatElapsed` exported but only used internally in `formatters.ts`.
- Coverage: 96.78% overall, all 158 files above 80% floor.

## Token Report — code-review
- Total tool calls: 22
- By tool: Bash: 7, Read: 9, Edit: 7, Grep: 2, Glob: 1, Skill: 1
- Largest Bash outputs: `git diff HEAD -- src/commands/run.ts` (~100 lines), `npx vitest run` (~30 lines), `npx codeharness coverage` (~5 lines)
- Files read: 11 unique, 12 total reads (no file read 3+ times)
- Redundant operations: none

### 6-1-evaluator-module-workspace-spawn — create-story (2026-04-03T03:40)

- **Epic naming collision:** `epic-6-retrospective.md` documents a previous sprint's Epic 6 (Dashboard Visualization Rework), not the current Epic 6 (Blind Evaluator & Verification). Subagent navigated past it.
- **Risk:** `EvaluatorVerdict` type already defined in `workflow-engine.ts` (story 5-2). Story 6-1's `EvaluatorResult` returns raw `output: string` — dev agent must avoid duplicating the verdict type (story 6-2 consolidates).
- **Risk:** Timeout via `Promise.race()` does not abort the underlying SDK session. Agent may continue running after timeout. Known architectural trade-off.
- **Observation:** Codebase already has significant evaluator infra in `workflow-engine.ts`. Story 6-1 is the "missing middle" — spawn lifecycle module.

## Token Report — create-story
- Total tool calls: 16
- By tool: Read: 8, Glob: 7, Grep: 1, Write: 1, Bash: 0, Edit: 0
- Files read: 10 unique, 10 total reads (no file read 3+ times)
- Redundant operations: none

### 6-1-evaluator-module-workspace-spawn — dev-story (2026-04-03T03:43)

- **Uncovered branches (83.33%):** Lines 142 and 168 show partially uncovered branches. These are `Date.now()` calls in timeout/success return paths — v8 coverage marks them as branch-relevant. Not a real gap; all code paths are exercised.
- **Timeout timer leak:** `setTimeout` in timeout promise is not cleared when dispatch resolves first. Minor leak in short-lived runs. Acknowledged trade-off.
- **`buildUnknownOutput` produces one finding per story file, not per AC:** Evaluator module doesn't parse story files to count ACs (that's story 6-2/6-3). Downstream consumers need to handle this.

## Token Report — dev-story
- Total tool calls: 14
- By tool: Bash: 6, Read: 7, Edit: 2, Grep: 1, Write: 2
- Largest Bash outputs: `npx tsc --noEmit` (~130 lines), `npx vitest --coverage` (~140 lines), `ls __tests__` (~35 lines)
- Files read: 7 unique, 7 total reads (no file read 3+ times)
- Redundant operations: none

### 6-1-evaluator-module-workspace-spawn — code-review (2026-04-03T03:50)

- **HIGH fixed:** Timer leak — `setTimeout` in `Promise.race` never cleared on success path. Could keep Node.js alive 5 min after evaluator completes.
- **MEDIUM fixed:** Non-Error rejection path untested (test added).
- **MEDIUM fixed:** Workspace creation failure path untested (test added).
- **LOW (not fixed):** `buildUnknownOutput` names findings `ac: index + 1` but maps to story files, not actual ACs. Misleading — downstream parsing (6-2) will handle.
- **LOW (not fixed):** `isDockerAvailable()` is synchronous `execFileSync` blocking event loop up to 10s. Acceptable for CLI.
- Coverage: 96.79% overall, evaluator.ts 100/92.85/100/100. Branch gap at line 160 unreachable in practice.

## Token Report — code-review
- Total tool calls: 18
- By tool: Bash: 7, Read: 8, Edit: 3, Grep: 1, Glob: 1, Skill: 1
- Largest Bash outputs: coverage run (~40 lines), test runs (~15 lines each)
- Files read: 10 unique, 10 total reads (no file read 3+ times)
- Redundant operations: none

### 6-1-evaluator-module-workspace-spawn — verification (2026-04-03T03:55)

- **AGENTS.md stale:** `codeharness verify` flagged AGENTS.md missing `evaluator.ts` from module listing. Documentation artifact issue, not AC failure.
- **Verification gap:** AC 3 partially relies on pre-compiled agent definition having `bare: true` and `source_access: false` — evaluator receives these, doesn't set them. By design per story dev notes.
- **Observation:** Branch coverage 92.85% — uncovered branch at line 160 is defensive `if (timeoutTimer)` guard, hard to trigger since timer always set.

## Token Report — verification
- Total tool calls: 14
- By tool: Bash: 8, Read: 2, Grep: 4, Write: 1
- Largest Bash outputs: `ls verification/` (~133 lines), `npx vitest run --coverage` (~60 lines), `npx vitest run` (~40 lines)
- Files read: 2 unique, 2 total reads
- Redundant operations: vitest run twice (full + evaluator-only), coverage run twice — could have combined
