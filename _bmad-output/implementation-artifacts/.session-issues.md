# Session Issues Log — 2026-04-02

### 1-1-delete-beads-integration — create-story (2026-04-02T07:58)

**Problems encountered:**
- `_bmad/bmm/config.yaml` was at `_bmad/config.yaml` — required a glob to find it
- workflow.md file referenced in skill trigger was actually workflow.yaml — minor naming inconsistency

**Workarounds applied:** None significant.

**Risks identified:**
- Some dependent files (bridge.ts, sync.ts, retro-import.ts, github-import.ts) may have their primary functionality gutted when beads calls are removed
- `src/modules/status/formatters.ts` and `src/modules/verify/orchestrator.ts` call `isBeadsInitialized` / `listIssues` — these may be in hot paths

**Observations:**
- Beads dependency graph is larger than epics-v2 doc suggests (25+ files total including tests)
- All 8 ACs are `test-provable` — pure code-deletion story

## Token Report
- Total tool calls: 16
- By tool: Bash: 4, Read: 8, Glob: 4, Grep: 1, Write: 1, Skill: 1
- Largest Bash outputs: `ls implementation-artifacts/` (~190 lines), `git log` (~5 lines)
- Files read: 8 unique, 8 total reads
- Redundant operations: None

### 1-1-delete-beads-integration — dev-story (2026-04-02T08:15)

**Problems encountered:**
- `init-project.test.ts` had `mockIsBeadsInitialized` in a `beforeEach` block not adjacent to mock declaration — required second test fix pass
- `status.test.ts` had health check test asserting `[OK] Beads: available` missed in first pass
- `retro-import.test.ts` and `github-import.test.ts` deeply intertwined with beads mocks — full rewrites necessary

**Workarounds applied:**
- Added local `BeadsIssue` interface in `src/lib/sync/story-files.ts` (import source was deleted)
- `epic-generator.test.ts` uses `describe.skip` for removed `importOnboardingEpic` test suite
- `storyToGapId` in `onboard-checks.ts` reimplemented with inline gap-id formatting

**Code quality concerns:**
- Several commands (bridge, sync, retro-import, github-import) are now partially hollow
- `getOnboardingProgress` and `filterTrackedGaps` are stubs returning hardcoded values
- retro-import and github-import test files lost significant coverage
- `--keep-beads` CLI flag still exists in teardown command

**Observations:**
- Story's 12-file dependency map was accurate — all files listed were touched
- Net LOC change is strongly negative as required by NFR17

## Token Report
- Total tool calls: 67
- By tool: Bash: 9, Read: 22, Edit: 28, Grep: 8, Write: 4, Glob: 1, Skill: 1
- Largest Bash outputs: `npm run test:unit` (~60 lines), `grep -r "from.*beads"` (~80 lines), `npm run test:unit` second run (~30 lines)
- Files read: 32 unique, 37 total reads
- Redundant operations: one unnecessary no-op Edit attempt; two test runs due to missed beads references

### 1-1-delete-beads-integration — code-review (2026-04-02T08:30)

**Bugs found and fixed (HIGH):**
- `SyncDirection` type in `story-files.ts` still had `'beads-to-files' | 'files-to-beads'` values — dead type members referencing deleted functionality
- `formatters.ts` health check emitted `{ name: 'beads', status: 'ok', detail: 'removed' }` — fake "OK" health check for non-existent feature
- `orchestrator.ts` module JSDoc said "updates state and beads" — stale contract claim

**Bugs found and fixed (MEDIUM):**
- `github-import.ts` JSDoc "Maps GitHub labels to a beads issue type" — stale beads reference
- `retro-import.ts` JSDoc "Failures here never affect beads import results" — stale beads reference
- `epic-generator.ts` module doc "and imports into beads" — stale beads reference

**Code quality issues found but NOT fixed (LOW):**
- `closeBeadsIssue` stub exported from `orchestrator.ts` — intentional no-op stub per story spec, tech debt for Epic 8
- `getOnboardingProgress` stub in `onboard-checks.ts` always returns `null` — same pattern
- Several AGENTS.md files still reference beads functions — documentation-only
- `describe.skip('importOnboardingEpic — removed')` in `epic-generator.test.ts` — dead test suite

**Coverage:** 96.69% overall, all 155 files above 80% floor.

## Token Report
- Total tool calls: 30
- By tool: Bash: 8, Read: 18, Edit: 9, Grep: 6, Glob: 2, Skill: 1
- Largest Bash outputs: `npm run test:unit` (~30 lines), `git status --porcelain` (~46 lines), `npx codeharness coverage` (~5 lines)
- Files read: 27 unique, 29 total reads
- Redundant operations: none significant

### 1-1-delete-beads-integration — verification (2026-04-02T09:38)

**Problems encountered:**
- `codeharness verify` failed on first attempt because coverage check hadn't been run in this session — needed `codeharness coverage --min-file 80` first
- Showboat not installed warning (non-blocking)
- No exec-plan found warning (non-blocking)

**Workarounds applied:**
- Ran coverage command before verify to satisfy preconditions

**Verification gaps:** None — all 8 ACs passed with clear evidence.

**Observations:**
- Proof document was already present from a previous session — this was a Tier A quick validation
- Story retry count was 1 from earlier session (below max of 10)

## Token Report
- Total tool calls: 6 (3 subagent invocations from parent)
- By tool: Bash: 4 (across subagents), Read: 3
- Largest Bash outputs: `npm run test:unit` (~10 lines tail), `codeharness coverage` (~10 lines tail)
- Files read: 4 unique (sprint-status.yaml, story file, proof, sprint-state.json)
- Redundant operations: Initial verify attempt failed due to missing coverage run — could be avoided by always running coverage first
