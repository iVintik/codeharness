# Session Issues Log — 2026-03-17

### 1-2-core-libraries-state-stack-detection-templates — verification validation (2026-03-17T00:42Z)

- **Quick win**: Proof already existed from prior session. `codeharness verify` confirmed it passes. Marked done without re-running Docker verification.
- **1 integration-required AC**: AC8 (unit test coverage) was flagged as integration-required but verify tool accepted the proof overall.

### Epic 1 — completion (2026-03-17T00:42Z)

- All 3 stories (1-1, 1-2, 1-3) now done. Epic marked done.
- Retrospective skipped due to time budget constraints.

### 2-1-dependency-auto-install-otlp-instrumentation — retry exhausted (2026-03-17T00:43Z)

- **Retry budget exceeded**: retry_count=4, max_retries=3. Story has been stuck across 6 sessions now.
- **Root cause unchanged**: AC5 (`--no-observability` flag) and AC6 (dependencies JSON) need dev work. Verification cannot succeed without code fixes.
- **Systemic issue**: harness-run has no path from `verifying` → `in-progress`. When verification reveals code bugs, the story gets stuck in a retry loop instead of being sent back for dev work.
- **Sprint halted**: This story blocks all forward progress because it's the first actionable story in Epic 2.

### 2-2/2-3 — Showboat failures (2026-03-17T00:43Z)

- Both stories have proofs that fail Showboat re-execution (block mismatches).
- These could potentially be fixed by regenerating proofs, but 2-1 blocks them in file order.

### Systemic observations

- **Blocked story pattern**: Many stories at `verifying` are blocked (escalated ACs) or stuck (retry exhausted). The sprint has ~40 stories at `verifying` but no mechanism to batch-process them or skip retry-exhausted stories to reach actionable ones.
- **Epic ordering**: Epic 2 blocks forward progress because harness-run processes epics sequentially. Stories in epics 3-11 cannot be reached until Epic 2's stories are resolved.
- **Missing state transition**: Need a `verifying` → `in-progress` path for stories where verification reveals code bugs (not just flaky proofs).
