# Session Issues Log — 2026-03-17

### 3-2-bmad-installation-workflow-patching — validation (2026-03-17T11:05:00Z)

No issues. Proof existed with all 12 ACs passing. Validated and marked done.

### 13-2-documentation-gate-for-verification — verification (2026-03-17T11:08:00Z)

- **Bug: `codeharness init` aborts on beads install failure** — beads is marked as "critical dependency" but is a pip package unavailable in Node.js-only environments. Init aborts before reaching README scaffold step. README.md is never created, making AC2 and AC3 fail.
- **Bug: JSON output lies about file creation** — init JSON reports `readme: "created"` even when init aborted before writing the file. JSON result is inconsistent with filesystem state.
- **Bug: `verify-env check` cliWorks is not live-testing** — reports "CLI works in container: yes" even after the codeharness binary is removed from the container. The check is not actually running `docker exec` to test CLI functionality.
- **Infrastructure: `claude --print` subprocess blocked by docker exec permissions** — verifier subprocess could not get permission to run docker exec commands, forcing manual verification.
- **Infrastructure: Harness docker-compose conflicts with shared stack on port 9428** — both compose files try to bind the same ports.
