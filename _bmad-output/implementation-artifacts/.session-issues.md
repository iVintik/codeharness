# Session Issues Log — 2026-03-23

### 8-1-rust-stack-and-app-type-detection — dev-story (2026-03-23T09:15Z)

- **Code quality concern:** Cargo.toml parsing uses simple string `includes()` matching, not a proper TOML parser. Per NFR4, but a dependency name in a comment or `[dev-dependencies]` would false-positive (e.g., `# we considered using axum` triggers server detection).
- **Observation:** Existing test for "unrecognized stack" was using `'rust'` as unrecognized — changed to `'java'`.
- **Observation:** AC4 maps library crates to `generic` AppType rather than new `'library'` type — deliberate design choice but means library crates are indistinguishable from unknown projects.

### 8-1-rust-stack-and-app-type-detection — code-review (2026-03-23T09:25Z)

- **HIGH fixed:** `[dev-dependencies]`/`[build-dependencies]` deps incorrectly matched as production deps — false positive agent/server detection. Added `getCargoDepsSection()` to extract only `[dependencies]` section.
- **HIGH fixed:** Substring false positives on crate names (`anthropic` matching `anthropic-sdk`, `rocket` matching `sprocket`). Added `hasCargoDep()` with word-boundary regex.
- **MEDIUM fixed:** `[[bin]]` and `[lib]` matched inside TOML comments. Used line-anchored regexes.
- **MEDIUM fixed:** Missing test coverage for 3 of 5 web frameworks (rocket, tide, warp).
- **MEDIUM fixed:** Missing test for `[lib]` + `[[bin]]` dual-crate pattern.
- **LOW unfixed:** `getCargoDepsSection()` does not handle `[dependencies.foo]` inline subsection TOML style. Acceptable per NFR4.
- **Coverage:** 97.02% overall, all 123 files above 80% floor. No integration test for detectStack→detectAppType end-to-end (acceptable — unit tested independently).
