# Session Issues Log — 2026-03-24

### 10-4-rust-provider — create-story (2026-03-24T09:14Z)

- **Ambiguity in epic definition:** Story notes mention `'library'` AppType for Rust, but `AppType` union in `src/lib/stacks/types.ts` has no `'library'` variant — only `'generic'`. Followed actual code behavior (returns `'generic'`).
- **Bevy system libs:** Story notes mention Bevy detection for Docker system libs, but no such logic exists in current `dockerfile-template.ts`. Deferred to avoid scope creep.
- **Risk — workspace aggregation ordering:** No cross-provider ordering concern in the provider pattern; handled within Rust provider alone.
- **Observation:** All 27 ACs are `cli-verifiable`. No external system dependencies for unit testing.

### 10-4-rust-provider — dev-story (2026-03-24T09:24Z)

No issues reported. 55/55 new tests pass, 3397/3397 total tests pass, no regressions.

### 10-4-rust-provider — code-review (2026-03-24T09:34Z)

- **MEDIUM (fixed):** Misleading JSDoc in `hasCargoDep` — example was backwards.
- **MEDIUM (fixed):** Missing false-positive prevention tests for crate substring matching (2 tests added).
- **LOW (not fixed):** `hasCargoDep` matches commented-out dependencies. Pre-existing behavior from `stack-detect.ts`.
- **LOW (not fixed):** `getCargoDepsSection` doesn't handle `[dependencies.foo]` subsection format. Pre-existing behavior.
- **Coverage gap:** No integration test for `installOtlp` success path (requires `cargo` binary).

### 10-5-migrate-consumers-to-stackprovider — create-story (2026-03-24T09:43Z)

- **Risk:** AC17 (`verify-prompt.ts`) compares `AppType` not `StackName` — could false-positive boundary tests.
- **Risk:** AC16 (`teardown.ts`) accesses state field not local variable — migration needs state shape understanding.
- **Risk:** `detectAppType()` export chain may break if `stack-detect.ts` deleted before re-export verified.
- **Observation:** `otlp.ts` is heaviest migration target (~10 stack conditionals). `scanner.ts` had zero stack conditionals.

### 10-5-migrate-consumers-to-stackprovider — dev-story (2026-03-24T09:55Z)

- **Coverage tool name mismatch (tech debt):** `StackProvider.getCoverageTool()` returns `CoverageToolName` values (`'tarpaulin'`, `'coverage-py'`) but `HarnessState.coverage.tool` uses different strings (`'cargo-tarpaulin'`, `'coverage.py'`). Docs-scaffold had to use hardcoded lookup instead of provider delegation.
- **RustProvider.getBuildCommands() duplication:** Returns `['cargo build', 'cargo test']` from `getBuildCommands()` and `['cargo test']` from `getTestCommands()`, causing `cargo test` to appear twice in generated AGENTS.md. Pre-existing provider design issue.
- **AGENTS.md format change:** Old hardcoded Rust section had inline comments and `cargo tarpaulin` line. Provider-based output omits these. Minor behavioral change, test updated.
- **verify-prompt.ts intentional exception:** Uses `projectType === 'nodejs'` (`PromptProjectType`, not `StackName`). Documented in boundary test's `ALLOWED_EXCEPTIONS` per AC17.
- **teardown.ts uses `.includes('nodejs')`:** String literal reference to stack name outside `src/lib/stacks/`, but acceptable per AC16 (array membership test allowed).

### 10-5-migrate-consumers-to-stackprovider — code-review (2026-03-24T10:03Z)

- **MEDIUM (fixed):** Unused import `_detectStack` in `src/lib/stacks/index.ts` — imported but never referenced.
- **MEDIUM (fixed):** Stale comment in `src/lib/stacks/types.ts` — referenced deleted `stack-detect.ts`.
- **LOW (not fixed):** `coverage.ts` uses local dispatch map instead of delegating to `provider.detectCoverageConfig()` — consumer `CoverageToolInfo` type differs from provider type. Future refactoring opportunity.
- **LOW (not fixed):** Multiple files use stack name strings as object keys in dispatch maps. Maintenance burden when adding 4th stack.
- **LOW (not fixed):** `verify/env.ts` `STACK_TO_PROJECT_TYPE` map is redundant given StackName/ProjectType value overlap.
- **Architecture concern:** `CoverageToolInfo` exists in two incompatible forms (consumer vs provider). Pre-existing, not introduced by this story.

### epic-10 — retrospective (2026-03-24T10:15Z)

- **CoverageToolName type fragmentation:** Provider values don't match HarnessState strings. Blocking clean provider adoption in coverage.ts. Needs tracking in Epic 14.
- **Story 10-5 density:** 17 ACs, 8 consumer files — heaviest story. Future migration epics should split by file count.
- **Dispatch maps half-measure:** Object key maps still enumerate all stacks explicitly, not caught by boundary test.
- **Pre-existing TS compilation errors (~100 lines):** Persisted across Epics 8, 9, 10. Prioritize 15-4-fix-ts-compilation-errors.
