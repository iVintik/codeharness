# Session Issues Log — 2026-03-18

### 2-3-status-report-one-screen-overview — create-story (2026-03-18T06:01:00Z)

- **Ambiguity: AC 5 action item labeling.** The epic definition says "shows with NEW/CARRIED labels" but doesn't define the boundary precisely. Used heuristic: items whose story appears in `run.completed` or `run.failed` are NEW; all others are CARRIED. May need refinement.
- **Risk: status.ts file size.** Existing `src/commands/status.ts` is 566 lines (exceeds 300-line NFR). Adding sprint state sections will make it larger unless logic stays in `reporter.ts`.
- **Observation: StatusReport type needs breaking extension.** Existing type is minimal; story requires significant additions.
- **Risk: FR30 (story drill-down) is story 2.4, not this story.** Output should include hint text but NOT implement drill-down.

### 2-3-status-report-one-screen-overview — dev-story (2026-03-18T06:10:00Z)

- **Pre-existing: `status.ts` already exceeds 300-line NFR18 limit** (was 567, now 642). Story says "do NOT increase it" but also requires adding sprint state display sections. All logic lives in `reporter.ts`, additions to status.ts are minimal (~75 lines).
- **Pre-existing: test isolation race between `index.test.ts` and `state.test.ts`** — both write to `sprint-state.json` in CWD. Nondeterministic collision when vitest runs them in parallel.
- **`RunSummary.skipped` is always empty** — no way to distinguish "blocked" from "skipped" in current state format.
- **No `--json` flag integration test** — JSON output path adds sprint data but no integration test exercises it end-to-end.

### 2-3-status-report-one-screen-overview — code-review (2026-03-18T06:29:00Z)

- **MEDIUM (fixed): `formatDuration()` negative duration on clock skew** — produced nonsensical output like "-1h-14m". Fixed with `Math.max(0, ms)` clamp.
- **MEDIUM (fixed): `getSprintReportData()` field drift risk** — manually copied 13 fields to plain `Record<string, unknown>`. Simplified to return `StatusReport` directly.
- **LOW (not fixed): `status.ts` at 629 lines** — still over NFR18 300-line limit. Pre-existing, needs decomposition story.
- **LOW (not fixed): `MAX_ATTEMPTS` hardcoded in `reporter.ts`** — duplicates constant from `selector.ts`.
- **Coverage gap: no integration test for full CLI `codeharness status`** against real sprint-state.json.
- **Architecture: `status.ts` is monolithic** — handles Docker, health, sprint state, beads, onboarding, coverage in one file.
