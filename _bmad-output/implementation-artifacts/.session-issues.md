# Session Issues Log — 2026-03-24 (session 3)

### 10-3-python-provider — create-story (2026-03-24T08:23Z)

- **Workaround:** Epic mentions CLI/library detection for Python app types, but codebase only has agent/web/server/generic. ACs reflect actual code, not aspirational epic text.
- **Risk:** `getProjectName()` uses TOML regex (fragile for edge cases). Proper TOML parser would be better but adds dependency.
- **Risk:** `hasPythonDep()` regex fragile for packages with hyphens/underscores (Python normalizes these).
- **Observation:** `PYTHON_OTLP_PACKAGES` in code are `opentelemetry-distro`/`opentelemetry-exporter-otlp`, not what the original story skeleton stated.
- **Observation:** Python has no `patchStartScript()` — uses `opentelemetry-instrument` wrapper via env vars instead.

### 10-3-python-provider — dev-story (2026-03-24T08:28Z)

No issues reported. 62 new tests pass, 3329 total, 0 regressions.

### 10-3-python-provider — code-review (2026-03-24T08:33Z)

- **HIGH (fixed):** `detectCoverageConfig` substring false positives — `String.includes('coverage')` matched `coverage-conditional-plugin`. Switched to `hasPythonDep()`.
- **MEDIUM (fixed):** `installOtlp` pipx fallback lost track of partially installed packages.
- **MEDIUM (fixed):** `getProjectName` TOML regex crossed section boundaries.
- **MEDIUM (fixed):** Missing direct tests for `getPythonDepsContent()` and `hasPythonDep()` utils — added 13 tests.
- **LOW (not fixed):** `hasPythonDep` matches package names inside comments — pre-existing behavior, low real-world impact.
- **LOW (not fixed):** Task comment markers in code could be cleaned up.
- **Coverage gap:** `installOtlp` success path untested (needs real pip/pipx binaries).

### 10-3-python-provider — verification (2026-03-24T08:38Z)

- **BLOCKER:** Docker Desktop is not running — `codeharness stack start` failed with "Docker Desktop is unable to start". Cannot perform black-box verification.
- **Observation:** All 21 ACs are tagged `cli-verifiable` (unit-testable). Story could benefit from `<!-- verification-tier: unit-testable -->` tag to skip Docker requirement for pure unit-test stories.
- **Action:** Story left at `verifying` for next session when Docker is available.

