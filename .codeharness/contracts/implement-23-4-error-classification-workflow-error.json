{
  "version": 1,
  "taskName": "implement",
  "storyId": "23-4-error-classification-workflow-error",
  "driver": "claude-code",
  "model": "claude-sonnet-4-6",
  "timestamp": "2026-04-05T19:07:59.730Z",
  "cost_usd": 1.2703612500000006,
  "duration_ms": 327553,
  "changedFiles": [
    "/Users/ivintik/dev/personal/codeharness/src/lib/workflow-types.ts",
    "/Users/ivintik/dev/personal/codeharness/src/lib/workflow-compiler.ts",
    "/Users/ivintik/dev/personal/codeharness/src/lib/workflow-actors.ts",
    "/Users/ivintik/dev/personal/codeharness/src/lib/workflow-machines.ts",
    "/Users/ivintik/dev/personal/codeharness/src/lib/__tests__/workflow-compiler.test.ts",
    "/Users/ivintik/dev/personal/codeharness/src/lib/__tests__/workflow-actors.test.ts"
  ],
  "testResults": null,
  "output": "Now let me check the existing tests:Let me read the relevant portions of the test files:Now I have a clear picture. Let me implement all the changes:Now add `isHaltError`, `isAbortError` to `workflow-compiler.ts` and update `handleDispatchError`:Now update `nullTaskCore` in `workflow-actors.ts` to throw `WorkflowError` instead of plain objects:Now update `workflow-machines.ts` — add `isHaltError` to imports, remove `HALT_ERROR_CODES`, fix halt detection, and fix the story-level null task halt bug:Now replace the halt detection in `loopIterationActor` (line 93):Now fix the story-level null task catch in `storyFlowActor` (adds halt detection and fixes the false story-done):Now fix the dispatch halt detection in `storyFlowActor`:Now fix the halt detection in `epicStepActor`:Now let me verify the changes look correct and check line counts:At 403 lines, I need to trim 3 lines from `workflow-machines.ts` to meet the ≤400 AC. Let me read the file to find compact opportunities:Let me compact the catch blocks in storyFlowActor to save 3 lines total:Need one more line reduction. Let me check the epicStepActor dispatch error catch:Now update the tests. First add tests to `workflow-compiler.test.ts` for `isHaltError` and `isAbortError`:Now add tests for `handleDispatchError` with `WorkflowError`, `isHaltError`, and `isAbortError` at the end of the file:Now add tests to `workflow-actors.test.ts` to verify `WorkflowError` instances are thrown. First, I need to check if `WorkflowError` needs to be imported there and what mock setup is needed:Now I'll add `WorkflowError` instanceof tests to the `workflow-actors.test.ts`. First, add the import:Now add instanceof tests after the nullTaskCore test block:Now let's build and run tests to check for errors:Build passes. Now run the tests:All 180 tests pass. Now run ESLint:Zero warnings. Run the full test suite to verify no regressions:All 5050 tests pass across 192 test files. Final verification:All constraints met. Here's what was implemented:\n\n**T1 — `WorkflowError` class** (`workflow-types.ts`): New `extends Error` class with `code`, `taskName`, `storyKey` fields. Sets `this.name = 'WorkflowError'` and captures native stack trace.\n\n**T2 — `isHaltError` / `isAbortError`** (`workflow-compiler.ts`): Both `DispatchError` and `WorkflowError` are checked against `HALT_ERROR_CODES`. `isAbortError` matches `err.name === 'AbortError'`. Also exports `WorkflowError` re-export for consumers.\n\n**T3 — Convert `nullTaskCore` throws** (`workflow-actors.ts`): All 3 plain-object throws replaced with `new WorkflowError(...)`. Errors now have stack traces and pass `instanceof` checks.\n\n**T4 — `handleDispatchError` handles `WorkflowError`** (`workflow-compiler.ts`): Extracts `taskName`/`storyKey`/`code` from the error's own fields.\n\n**T5 — Fix story-level null task halt** (`workflow-machines.ts`): The `storyFlowActor` null task catch now calls `isHaltError(err)` and sets `halted = true` for halt-worthy codes — preventing the false `story-done` emission.\n\n**T6 — Unified halt detection** (`workflow-machines.ts`): All three `instanceof DispatchError && HALT_ERROR_CODES.has(err.code)` checks replaced with `isHaltError(err)`. Removed unused `DispatchError` and `HALT_ERROR_CODES` imports.\n\n**T7 — Tests**: Added `isHaltError`/`isAbortError` tests to `workflow-compiler.test.ts`, `WorkflowError instanceof` and stack trace tests to `workflow-actors.test.ts`, and `handleDispatchError(WorkflowError)` tests.",
  "acceptanceCriteria": [
    {
      "id": "AC1",
      "description": "**Given** a workflow with a null task `telemetry` whose handler throws an exception, **When** `codeharness run` executes that task, **Then** the terminal output or logs show an error with a classification code (`NULL_TASK_HANDLER_ERROR`) and the task name `telemetry`, AND the error includes a stack trace (visible in debug/verbose mode or log file). <!-- verification: register a throwing null task handler, run `codeharness run`, observe error output contains \"NULL_TASK_HANDLER_ERROR\", \"telemetry\", and a stack trace line with \"at\" -->",
      "status": "pending"
    },
    {
      "id": "AC2",
      "description": "**Given** a workflow with a null task referencing an unregistered handler, **When** `codeharness run` reaches that task, **Then** the terminal output shows an error with code `NULL_TASK_NOT_FOUND`, AND `codeharness status` or the workflow state file shows the workflow halted — it did NOT continue to the next task or next story. <!-- verification: configure workflow with unregistered null task, run `codeharness run`, confirm error with \"NULL_TASK_NOT_FOUND\"; then `codeharness status` or `cat .codeharness/workflow-state.yaml` shows phase is \"error\" or halted indicator -->",
      "status": "pending"
    },
    {
      "id": "AC3",
      "description": "**Given** a workflow where a null task fails at the story level (inside story flow, not in a loop), **When** the null task error is processed, **Then** the workflow state file shows the story DID NOT emit a `story-done` event for that story key, AND the terminal output does NOT show a `story-done` log for that story. <!-- verification: run workflow where story-level null task fails, grep logs for \"story-done\" with that story key — expect zero matches; inspect workflow state for halted indicator -->",
      "status": "pending"
    },
    {
      "id": "AC4",
      "description": "**Given** a workflow where a dispatch task fails with error code `RATE_LIMIT`, **When** the error is processed, **Then** `codeharness status` or the workflow state file shows the workflow entered a halted/error state, AND the error record in the state file contains `code: \"RATE_LIMIT\"` and the task name. <!-- verification: trigger rate limit error (invalid API key or mock), run `codeharness run`, check `codeharness status` for halted state; inspect state file errors array for code=RATE_LIMIT entry -->",
      "status": "pending"
    },
    {
      "id": "AC5",
      "description": "**Given** a workflow where a dispatch task fails with error code `NETWORK`, **When** the error is processed, **Then** the workflow halts (same behavior as RATE_LIMIT — the workflow does NOT continue to the next task). <!-- verification: trigger network error, run `codeharness run`, confirm workflow stopped and state file shows error state -->",
      "status": "pending"
    },
    {
      "id": "AC6",
      "description": "**Given** a workflow where a dispatch task fails with error code `UNKNOWN`, **When** the error is processed, **Then** the error is recorded in the workflow state file's error entries, AND the workflow halts the current story but the epic continues to the next story (non-fatal for the overall run). <!-- verification: trigger unknown error, inspect state file for error entry with code=UNKNOWN; observe that subsequent stories in the same epic are attempted -->",
      "status": "pending"
    },
    {
      "id": "AC7",
      "description": "**Given** a running workflow that receives SIGINT (Ctrl+C), **When** a dispatch is in progress, **Then** the running driver subprocess is killed, the workflow state is persisted to disk with a recent timestamp, AND the state file shows a phase of `interrupted` (not `error` or `executing`). <!-- verification: start `codeharness run`, send SIGINT during active dispatch, inspect state file for phase=\"interrupted\" and recent timestamp -->",
      "status": "pending"
    },
    {
      "id": "AC8",
      "description": "**Given** a workflow where multiple tasks fail across different stories, **When** the entire run completes, **Then** `codeharness status` shows all errors with their task names, story keys, and classification codes — no errors are swallowed or missing from the summary. <!-- verification: run workflow that hits multiple errors, then `codeharness status` or inspect state file — count error entries matches expected count, each has taskName/storyKey/code -->",
      "status": "pending"
    },
    {
      "id": "AC9",
      "description": "**Given** a workflow with a null task failure followed by subsequent stories, **When** the error occurs in story N, **Then** the workflow state file shows an error record for story N's null task, AND story N+1 begins execution (null task failure in one story does not halt the entire epic — unless the error code is in the halt set). <!-- verification: run workflow with 2+ stories where story 1 has a failing null task with code NULL_TASK_FAILED (non-halt code), observe story 2 is attempted; inspect state for error entry for story 1 -->",
      "status": "pending"
    },
    {
      "id": "AC10",
      "description": "**Given** the project is built via `npm run build`, **When** the build completes, **Then** it exits 0 with no TypeScript errors. <!-- verification: `npm run build` exits 0 -->",
      "status": "pending"
    },
    {
      "id": "AC11",
      "description": "**Given** the full test suite is run via `npx vitest run`, **When** all tests complete, **Then** all tests pass with zero failures — no regressions from error classification changes. <!-- verification: `npx vitest run` exits 0 with 0 failures -->",
      "status": "pending"
    },
    {
      "id": "AC12",
      "description": "**Given** `workflow-actors.ts` exists on disk, **When** its line count is checked, **Then** it contains ≤ 300 lines (per NFR18 file size constraint). <!-- verification: `wc -l src/lib/workflow-actors.ts` shows ≤ 300 -->",
      "status": "pending"
    },
    {
      "id": "AC13",
      "description": "**Given** `workflow-machines.ts` exists on disk, **When** its line count is checked, **Then** it contains ≤ 400 lines (current ~402 lines, target ≤ 400 after cleanup). <!-- verification: `wc -l src/lib/workflow-machines.ts` shows ≤ 400 -->",
      "status": "pending"
    }
  ]
}
