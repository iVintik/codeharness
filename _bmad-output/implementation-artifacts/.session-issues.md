# Session Issues Log — 2026-03-18

### 3-2-graceful-dev-module — verification (2026-03-18T18:05Z)

Fixed two bugs in proof.ts that blocked verification of unit-testable stories:
1. AC section parsing bled into ## Summary, causing last AC to pick up [ESCALATE] from summary table
2. Black-box enforcement applied to unit-testable proofs where docker exec is irrelevant

### 3-3-verify-dev-feedback-loop — verification (2026-03-18T18:05Z)

Same proof.ts bugs blocked this story. Fixed alongside 3-2.

### 4-1-verify-module-extraction — code-review (2026-03-18T18:10Z)

- HIGH: proof.ts at 303 lines exceeded 300-line NFR18 limit — FIXED
- HIGH: commands/verify.ts at 303 lines exceeded 300-line NFR18 limit — FIXED
- MEDIUM: verify.test.ts had 4 TypeScript errors due to mock objects using wrong property names — FIXED
- LOW (not fixed): perAC[].verified always hardcoded to true regardless of escalation status
- LOW (not fixed): Hardcoded paths duplicated across modules
- Architecture: index.ts facade at 141 lines approaching soft limit

### 4-2-project-agnostic-verification — create-story (2026-03-18T18:18Z)

- Architecture doc has minimal detail about project-agnostic verification strategy
- Epic has 3 ACs but implementation needed 11 ACs for proper granularity
- Risk: proof.ts at 288-299 lines — any spillover hits 300-line NFR18 limit
- Risk: buildVerifyImage() currently throws instead of returning Result<T> — AC references Result<BuildResult> which requires signature change
- Risk: No existing detectStack() return value for plugins — may need extension

### 4-2-project-agnostic-verification — dev-story (2026-03-18T18:28Z)

- Coverage gap on env.ts: 93% statement coverage. Uncovered lines (247-250, 269-271) are pre-existing error paths from Story 4.1
- Plugin image uses generic Dockerfile instead of dedicated plugin Dockerfile — architecturally imprecise but functional
- detectProjectType placed in env.ts rather than stack-detect.ts to keep changes minimal per story scope
- PromptProjectType duplicates ProjectType — both are 'nodejs' | 'python' | 'plugin' | 'generic', could be unified later

### 4-2-project-agnostic-verification — code-review (2026-03-18T18:34Z)

- HIGH (fixed): Dockerfile.verify.generic missing COPY . /workspace/ — plugin/generic verification images would be empty
- HIGH (fixed): env.ts coverage at 93.46% vs 100% requirement — added tests for getImageSize branches, resolveDockerfileTemplate error, Python empty dist/, state read/write failures
- MEDIUM (fixed): 5 `as any` casts in test file violating NFR19
- LOW (not fixed): env.ts lines 54,61 use double-cast `as unknown as Record` to access verify_env_dist_hash — HarnessState type should be extended
- LOW (not fixed): buildVerifyImage() throws instead of returning Result<BuildResult> per architecture Decision 1
- Coverage: env.ts branch at 94.38% — uncovered branches in buildPythonImage .whl fallback and resolveDockerfileTemplate pkg-level fallback

---

# Session Issues Log — 2026-03-18 (session 8)

### 4-3-verifier-session-reliability — create-story (2026-03-18T18:41Z)

- AC #2 (nested --allowedTools in Docker) tagged integration-required — requires Docker+Claude to verify
- AC #8 (sprint loop hang recovery) requires end-to-end integration with ralph's sprint loop
- verifier-session.ts lives in lib/ not modules/verify/ — story scoped to reliability, not reorganization
- proof.ts at 288-299 lines — approaching 300-line ceiling
- Epic ACs expanded from 4 bullet points to 10 concrete ACs — significant product decisions made by story author

### 4-3-verifier-session-reliability — dev-story (2026-03-18T18:48Z)

- Pre-existing flaky test: sprint/state.test.ts > updateStoryStatus > attaches error detail — fails intermittently in full suite
- Test isolation issue: vi.clearAllMocks() does not reset mockImplementation() — had to use vi.restoreAllMocks() + manual mockReset()
- Branch coverage at 95.91%: two uncovered branches (lines 183, 256) are optional chaining short-circuits in error handler
- VerifierSessionResult type removed (dead code) — no external consumers found
- copyProofToProject still throws instead of Result<T> — out of scope but tech debt

### 4-3-verifier-session-reliability — code-review (2026-03-18T18:53Z)

- HIGH (fixed): Module boundary violation — verifier-session.ts imported directly from env.js and types.js instead of through index.js
- MEDIUM (fixed): Stale AGENTS.md in src/lib/ — documented removed type VerifierSessionResult
- MEDIUM (fixed): Stale AGENTS.md in src/modules/verify/ — missing cleanupStaleContainers export
- LOW (not fixed): copyProofToProject() still throws instead of Result<T>
- LOW (not fixed): projectDir is unused parameter in VerifierSessionOptions
- Coverage: proof.ts function coverage at 90.9%, verify/index.ts branch at 58.33% — both pre-existing
