# Session Issues Log — 2026-03-18

### 2-4-status-story-drill-down — create-story (2026-03-18T06:50:00Z)

- **Workaround: StoryState lacks per-attempt history.** Current type only tracks `attempts: number`, not per-attempt logs. Drill-down will show synthetic attempt entries rather than real historical data.
- **Workaround: AcResult lacks detail fields.** Only has `id` and `verdict` — no command/expected/actual/reason/suggestedFix. Story recommends extending with optional fields.
- **Risk: AC #2 FAIL detail depends on enriched AcResult data** that the verifier must populate. If verifier doesn't write these fields, drill-down shows verdicts without detail.
- **Risk: AC #3 attempt history will be approximate** since StoryState doesn't store per-attempt logs.
- **Observation: reporter.ts at 203 lines** — adding getStoryDrillDown() should stay under 300 but will be close.

### 2-4-status-story-drill-down — dev-story (2026-03-18T07:00:00Z)

- **Pre-existing tech debt: status.ts now 714 lines** (was 629, NFR18 limit is 300). Not introduced by this story but worsened.
- **AcDetail enrichment limited:** Only `reason` populated from `lastError` for failed ACs. Other detail fields (command, expected, actual, suggestedFix) await upstream AcResult extension.
- **Attempt history is synthetic:** StoryState only has `attempts: number`, not per-attempt logs. Historical attempts show "details unavailable".
- **Harmless stderr noise:** Two Commander option parsing errors during test runs — not failures, just import-time side effects.

### 2-4-status-story-drill-down — code-review (2026-03-18T07:15:00Z)

- **HIGH: `AcDetail.verdict` typed as `string` instead of `AcVerdict`** — loses type safety. Fixed.
- **HIGH: `StoryDrillDown.status` typed as `string` instead of `StoryStatus`** — same issue. Fixed.
- **MEDIUM: `ProofSummary` missing `pendingCount`** — proof total was wrong when pending ACs existed. Fixed.
- **LOW (not fixed): `MAX_ATTEMPTS` alias duplicated** in drill-down.ts and reporter.ts — minor, both one-liners.
- **LOW (not fixed): `status.ts` at 714 lines** exceeds 300-line NFR18 limit — pre-existing tech debt.
- **LOW (not fixed): `AttemptRecord.outcome` typed as `string`** instead of union type.
- **Coverage: drill-down.ts branch coverage 86.84%** — uncovered defensive paths on lines 26, 61, 91, 96, 134.

### 3-1-error-capture-on-timeout — create-story (2026-03-18T07:22:00Z)

- **Problem: Two epics files exist** (`epics.md` and `epics-overhaul.md`) — had to determine `epics-overhaul.md` is correct based on sprint-status.yaml story key alignment.
- **Workaround: Expanded sparse ACs** — epic definition had only 3 ACs; expanded to 6 to cover full scope (Result<T>, error handling, performance, status integration).
- **Risk: AC 6 (status drill-down)** crosses module boundaries to story 2.4's work — may need stories 3.2/3.3 to be fully testable. Tagged `integration-required`.
- **Risk: npx startup time** — `npx codeharness timeout-report` may exceed 10-second budget in AC #5 if npm/npx is slow.
- **Observation: First story modifying both ralph bash code and TypeScript modules** — dev agent needs dual-language handling.

### 3-1-error-capture-on-timeout — dev-story (2026-03-18T07:28:00Z)

- **AC #6 gap:** AC #6 requires `codeharness status --story` to show timeout report in drill-down. None of the 10 tasks implement this integration (would require storing timeout report paths in sprint-state.json and modifying drill-down reporter). Gap between ACs and task breakdown.
- **Pre-existing BATS failures:** 20+ BATS tests in `bridge.bats` and `exec_plans.bats` fail due to `declare -A` requiring bash 4+ on macOS. Unrelated to this story.
- **npx dependency in ralph.sh:** Timeout capture uses `npx codeharness timeout-report` — adds overhead. Direct `node dist/index.js timeout-report` would be more efficient but requires knowing install path.

### 3-1-error-capture-on-timeout — code-review (2026-03-18T07:46:00Z)

- **HIGH: Zero coverage on `src/commands/timeout-report.ts`** (4.76% statements, 0% branches). Fixed by adding 7 unit tests.
- **MEDIUM: Story key not sanitized in timeout report filename** — path traversal/malformed filenames possible. Fixed.
- **MEDIUM: Trailing newline off-by-one in `capturePartialStderr`** — last captured line wasted on empty string. Fixed.
- **MEDIUM: No validation on `iteration`/`durationMinutes` parameters** — negative/zero/non-integer values accepted silently. Fixed.
- **LOW (not fixed): `capturePartialStderr` reads entire file into memory** before slicing last N lines.
- **LOW (not fixed): `captureTimeoutReport` hardcodes `sprint-state.json` path** via `process.cwd()`.
- **LOW (not fixed): `formatReport` hardcodes "last 100 lines"** in heading regardless of actual maxLines.
- **LOW (not fixed): Unused `dirname` import** in timeout.ts line 8.
- **Coverage gap:** `captureStateDelta` does not test malformed JSON parse error path.
- **Architecture concern:** AC #6 (status drill-down showing timeout info) not implemented — needs follow-up story.

### 3-2-graceful-dev-module — verification (2026-03-18T08:18:00Z)

- **Story blocked:** 2 escalated ACs (AC 6 is `integration-required`, second escalation reason unknown — possibly codeharness verify auto-escalates integration-required ACs from the story file).
- **All 1861 tests pass, build clean.**
- **Dev module at 100% coverage** on all metrics.
- **7 of 8 ACs verified at unit-testable tier** — only AC 6 (sprint loop continues on failure) requires integration testing with live ralph run.
- **Proof document quality gap:** `codeharness verify` reports 6 verified vs my manual count of 7 PASS. Parser may have issues detecting PASS verdicts in certain AC section formats.

### 3-2-graceful-dev-module — code-review (2026-03-18T08:12:00Z)

- **HIGH: Missing AGENTS.md** for `src/modules/dev/` — required by project convention. Fixed: created file.
- **HIGH: Missing proof document** — created `verification/3-2-graceful-dev-module-proof.md`.
- **MEDIUM: `filesChanged`/`testsAdded` not captured in non-timeout error path** — partial work data lost on non-timeout failures. Fixed in orchestrator.ts.
- **MEDIUM: Branch coverage gap on line 59** — empty line filtering untested. Fixed: added test, now 100%.
- **MEDIUM: SIGTERM-only timeout path untested** — `signal === 'SIGTERM'` without `killed === true`. Fixed: added test.
- **LOW (not fixed): `Record<string, unknown>` casts** used 3 times in orchestrator.ts for untyped child_process error properties.
- **LOW (not fixed): `captureFilesChanged()` uses `execSync`** instead of `execFileSync` for git commands. Inconsistent but low risk.
- **LOW (not fixed): Test redundancy** between index.test.ts and orchestrator.test.ts for Result shape testing.
- **Code review created proof document prematurely** — this should be verification's job, not code review's. May cause confusion in verification tier classification.

### 3-2-graceful-dev-module — dev-story (2026-03-18T08:05:00Z)

- **Branch coverage gap:** 96.96% branch coverage on orchestrator.ts line 59 — empty-string guard in git file list parsing. V8 coverage reports false-branch uncovered because split('\n') on non-empty strings doesn't produce empty entries in test data. Defensive guard, not worth adding a test for.
- **No integration test with real `claude` CLI:** Orchestrator invokes `claude --print` which cannot be tested without real CLI. All tests mock `execFileSync`. AC #6 (sprint loop continues on failure) covered by Result<T> contract but true integration testing requires a live ralph run.
- **Output capture limitation:** `execFileSync` captures stdout but stderr only available in error object on failure. Story spec says "captured stdout/stderr summary" but execFileSync with encoding returns only stdout on success. Matches timeout.ts pattern.

### 3-2-graceful-dev-module — create-story (2026-03-18T08:00:00Z)

- **Workaround: Expanded sparse ACs** — epic definition had only 3 ACs; expanded to 8 to cover NFRs (file size, coverage, module boundary enforcement). Original 3 ACs map to ACs #1-#3.
- **Risk: AC #1 and #5 reference "invokes BMAD dev-story"** but exact invocation mechanism is ambiguous. Current system uses `/bmad-dev-story` as a Claude Code skill within a Claude session — not a standalone CLI command. Orchestrator may need `claude --print` with a skill-triggering prompt.
- **Risk: AC #6 (integration-required)** genuinely hard to test in isolation — requires ralph running a full loop with dev module producing a failure.
- **Observation: Existing dev module is a pure stub** (`fail('not implemented')`) with minimal `DevResult` type missing `duration` and `output` fields.
- **Observation: Story 3.1's timeout.ts provides a clean pattern** for `execSync` with timeout + Result wrapping to mirror.

### 3-1-error-capture-on-timeout — verification (2026-03-18T08:00:00Z)

- **Bug in verify tool: blackBoxPass enforcement applies to unit-testable proofs.** The `checkBlackBoxEnforcement` function requires docker-exec/docker-host/observability commands in EVERY non-escalated AC section. This is correct for black-box proofs but breaks unit-testable proofs which use `npx vitest`, `npx tsx`, etc. The evidence detector (`hasEvidence`) requires ```bash blocks, but the command classifier also extracts from ```bash blocks, creating an unavoidable conflict. Proof correctly shows verified=5, pending=0, escalated=1 but `passed=false` due to blackBoxPass.
- **AC 6 correctly escalated:** Requires integration between timeout module and status drill-down reporter. Genuinely cannot be verified without cross-module work.
- **Story blocked:** Stays at `verifying` due to 1 escalated AC. Step 2 skip logic will advance past it in future sessions.

### 3-3-verify-dev-feedback-loop — dev-story (2026-03-18T08:36:00Z)

- **Workaround:** Used `vi.resetAllMocks()` instead of `vi.clearAllMocks()` in tests due to mock leaks with persistent `mockImplementation`.
- **Branch coverage 88.63%:** Uncovered branches are regex match arms in `parseProofForFailures` — contrived proof formats unlikely in practice. Statement/function 100%.
- **AC #2 not testable in this story:** Dev prompt inclusion is handled at harness-run prompt level, not TypeScript. By design.
- **sprint-status.yaml not updated by dev agent:** Had to manually sync `ready-for-dev → review`.

### 3-3-verify-dev-feedback-loop — create-story (2026-03-18T08:25:00Z)

- **Ambiguity in AC #2 scope:** Epic says "dev prompt includes failing ACs" but this is handled at harness-run prompt level, not in TypeScript. Tagged integration-required.
- **Workaround for proof parsing:** Verify module is still a stub; feedback.ts needs its own proof parser (duplicate logic that will eventually live in Epic 4).
- **Risk: AC #3 threshold ambiguity** — "N >= 10 cycles" must check `attempts >= 10` AFTER increment. Matches harness-run `max_attempts = 10`.
- **Observation:** sprint-state.json attempt tracking already auto-increments on `updateStoryStatus(key, 'in-progress')` — feedback loop just checks the count.
