# Session Issues Log — 2026-03-18 (Session 4)

### 2-2-story-selection-cross-epic-prioritization — create-story (2026-03-18T05:32:00Z)

- Ambiguity in epic definition: AC #1 lists `proof-exists` as highest priority, but `in-progress` should logically take precedence. Resolved by making `in-progress` priority 0 (highest) and `verifying-with-proof` priority 1.
- Missing title source: `StorySelection.title` required by type but `SprintState` does not store story titles. Dev will need to derive from key or read story markdown files.
- `ready` status exists in `StoryStatus` union but not mentioned in epic priority tiers or PRD FR7. Left unspecified — dev should treat as equivalent to `backlog`.

### 2-2-story-selection-cross-epic-prioritization — dev-story (2026-03-18T05:37:00Z)

None.

### 2-2-story-selection-cross-epic-prioritization — code-review (2026-03-18T05:51:00Z)

- HIGH: AC #2 not implemented — selectNextStory() silently skipped retry-exhausted stories without reporting them or updating status to blocked. Fixed by returning RetryExhaustedInfo[] in SelectionResult.
- MEDIUM: Dead code in priorityTier() — attempts >= MAX_STORY_ATTEMPTS guard unreachable after retry-exhaustion moved to caller. Removed.
- MEDIUM: Missing test coverage for non-Error throws and empty key edge case. Added tests.
- MEDIUM: Coverage report directory misconfigured — vitest output went to src/coverage/ instead of coverage/. Fixed reportsDirectory in vitest.config.ts.
- LOW (not fixed): priorityTier default branch uncoverable (defensive guard for unknown status values)
- LOW (not fixed): getNextStory line 35 selector fail path hard to trigger through integration
- Architecture concern: getNextStory() now has side effects (reads AND writes state), mixing query and command. Acceptable per AC requirements.
