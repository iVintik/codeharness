# Session Issues Log — 2026-03-19

### 6-3-non-interactive-bmad-install — verification (2026-03-19T01:17Z)

**Problems encountered:**
- Verifier session (`claude --print`) timed out after ~8 minutes without producing a proof document. The verifier was likely blocked on `codeharness init` execution inside the container (which runs `npx bmad-method install --yes --tools claude-code` and that install takes a long time or hangs in Docker).
- Container did not have codeharness CLI pre-installed. Had to manually `docker cp` dist/ and `npm install` dependencies, then symlink the binary. `verify-env build` does not install the project CLI.
- Observability stack was down at session start, required `codeharness stack start`.

**Workarounds applied:**
- Manual CLI installation in container (same workaround as previous sessions).
- Stopped verifier session to respect time budget.

**Infrastructure issues:**
- `verify-env build` consistently produces containers without the project CLI installed. This is the root cause of repeated workarounds across sessions. The Dockerfile template needs to install the CLI from the built dist/.
- `claude --print` verifier sessions running `codeharness init` inside Docker may hang because `npx bmad-method install` downloads packages at runtime, which can be slow or fail in containers.

**Outcome:**
- No proof produced. Infrastructure retry 1/10. Story stays at `verifying`.

### 6-3-non-interactive-bmad-install — verification (2026-03-19T01:39Z)

**Problems encountered:**
- Verifier session (`claude --print`) timed out again after ~9 minutes without producing a proof document.
- The verifier process ran in background but never completed within the 540s TaskOutput timeout.
- No proof file was written — verifier was likely still working through initial AC verification steps when it ran out of time/budget.

**Infrastructure issues:**
- Same root cause as previous attempt: `claude --print` verifier sessions are too slow for this story. The story requires running `codeharness init` inside Docker, which triggers `npx bmad-method install` — a slow network-dependent operation.
- The $5 budget may be insufficient for a verifier session that needs to run multiple `codeharness init` invocations with different scenarios (fresh install, already installed, failure simulation, bmalph detection, etc.).

**Recommendations:**
- Consider tagging this story as `unit-testable` — all 11 cli-verifiable ACs test internal function behavior that is fully covered by unit tests. Black-box verification of internal function signatures (AC 4, 5) is impossible anyway.
- Alternatively, increase verifier budget or pre-install bmad-method in the Docker image to avoid runtime npm downloads.

**Outcome:**
- No proof produced. Infrastructure retry 2/10. Story stays at `verifying`.
