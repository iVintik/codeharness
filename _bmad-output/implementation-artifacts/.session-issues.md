# Session Issues Log — 2026-03-17

### 3-2-bmad-installation-workflow-patching — validation (2026-03-17T11:05:00Z)

No issues. Proof existed with all 12 ACs passing. Validated and marked done.

### 13-2-documentation-gate-for-verification — verification (2026-03-17T11:08:00Z)

- **Bug: `codeharness init` aborts on beads install failure** — beads is marked as "critical dependency" but is a pip package unavailable in Node.js-only environments. Init aborts before reaching README scaffold step. README.md is never created, making AC2 and AC3 fail.
- **Bug: JSON output lies about file creation** — init JSON reports `readme: "created"` even when init aborted before writing the file. JSON result is inconsistent with filesystem state.
- **Bug: `verify-env check` cliWorks is not live-testing** — reports "CLI works in container: yes" even after the codeharness binary is removed from the container. The check is not actually running `docker exec` to test CLI functionality.
- **Infrastructure: `claude --print` subprocess blocked by docker exec permissions** — verifier subprocess could not get permission to run docker exec commands, forcing manual verification.
- **Infrastructure: Harness docker-compose conflicts with shared stack on port 9428** — both compose files try to bind the same ports.

### 13-3-black-box-verifier-agent — verification (2026-03-17T11:51:00Z)

- **Infrastructure: `claude --print` verifier timed out after 10 minutes** — the verifier subprocess ran for the full 10-minute timeout without producing a proof document. No output was captured. This is the 2nd infrastructure failure for this story (retry 2/3).
- **Root cause hypothesis:** The verification prompt for story 13-3 requires testing the verifier system itself (meta-verification). Several ACs require spawning nested `claude --print` sessions inside Docker, which may cause the outer `claude --print` to exceed the 10-minute budget.
- **Observation:** Stories that test the verification infrastructure itself are inherently harder to verify within the standard timeout because they require nested Claude sessions.

### 13-3-black-box-verifier-agent — verification (2026-03-17T11:58:00Z)

- **Infrastructure: `claude --print` subprocess blocked by docker exec permissions (attempt 3/3)** — the verifier session ran but could not execute `docker exec` or `curl` commands due to permission prompts in non-interactive `--print` mode. The session returned a message asking for permission approval instead of producing a proof document.
- **Root cause confirmed:** `claude --print` mode does not auto-approve Bash tool calls (docker exec, curl). The verifier needs pre-approved permissions or an allowlist to function. This is a systemic issue affecting ALL black-box verification — not just meta-verification stories.
- **Story exhausted retry budget (3/3)** — marked as skipped (infra-retry-exhausted).

### 13-4-verification-environment-sprint-workflow — verification (2026-03-17T12:05:00Z)

- **Fix: `--allowedTools` flag resolves permission blocking** — adding `--allowedTools 'Bash Read Write Glob Grep'` to the `claude --print` invocation allows the verifier to run docker exec and curl without permission prompts. This is the fix for the systemic issue that blocked 13-3.
- **Proof quality initial failure: AC 6 lacked functional evidence** — the verifier produced text-only evidence for the timeout AC (no bash/output code blocks). Required manual proof fix to add docker exec evidence.
- **All 7 ACs passed** after proof fix. Verified and marked done.

### 2-2-docker-compose-victoriametrics-stack-management — verification (2026-03-17T12:10:00Z)

- **Bug: AC 6 — Init exits 0 when Docker absent** — AC requires exit code 1 and `[FAIL]`. Implementation uses graceful degradation (`[WARN]`, exit 0). Story returned to dev.
- **Bug: AC 7 — No `docker` object in JSON output** — `codeharness init --json` omits the docker object entirely.
- **Bug: AC 8 — Trace pipeline protocol mismatch** — gRPC `otlp/traces` exporter targets Jaeger HTTP port 14268, causing `http2: frame too large`. Log and metric routing work.
- **Key discovery: `--allowedTools` flag fixes permission blocking** — used `claude --print --allowedTools 'Bash Read Write Glob Grep'` successfully. This should be applied to all future verification runs.

### 2-2-docker-compose-victoriametrics-stack-management — dev-story (2026-03-17T12:23:00Z)

- **Observation:** The remote OTel template (`otelCollectorRemoteTemplate`) still uses `otlp/traces` (gRPC) for user-provided traces URL. Creates inconsistency with local template fix. Remote users with HTTP-only endpoints will hit the same protocol mismatch.
- **Observation:** The Docker-not-available `else` branch is now effectively unreachable code when observability is ON (AC 6 returns early). Remains as defensive fallback — could be cleaned up later.
- **No other problems, workarounds, or quality concerns.**

### 2-2-docker-compose-victoriametrics-stack-management — code-review (2026-03-17T12:30:00Z)

- **HIGH fixed:** Remote OTel template used gRPC exporter (`otlp/traces`) for traces — same protocol mismatch as AC 8. Fixed to `otlphttp/traces`.
- **HIGH fixed:** Idempotent re-run JSON output omitted `docker` object. Fixed to populate from existing state.
- **LOW unfixed:** `DockerComposeConfig.shared` field declared but never read inside `dockerComposeTemplate()`.
- **LOW unfixed:** `init.ts` is 780+ lines — god-function pattern. Each story adds 30-50 lines.
- **Coverage:** 95.21% overall, all files above 80% floor. No integration tests exist (unit-level mocks only).
