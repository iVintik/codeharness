# Session Issues Log — 2026-03-20 (Session 7)

### 0-5-4-run-command-integration — dev-story (2026-03-20T08:12Z)

- **run.ts at exactly 300 lines**: Was 358 originally, NFR9 requires ≤300. Extracting `countStories` and `buildSpawnArgs` to run-helpers.ts got it to exactly 300. Any future changes need more extraction.
- **Pre-existing TS errors in test files**: Loosely-typed `vi.fn()` mocks don't match strict type signatures. Exists across many test files — not introduced by this story.
- **Coverage gap in polling interval**: The 5-second `setInterval` callback in run.ts (lines 207-228) is not directly tested. Helper functions it calls are tested independently with 100% coverage.
- **run-helpers.test.ts at 303 lines**: Slightly over 300-line NFR9 limit. Test files typically exempt.

### 0-5-4-run-command-integration — code-review (2026-03-20T08:20Z)

- **MEDIUM: Misleading test name** (`defaults to 3` but asserts `10`) — fixed.
- **LOW (not fixed): run.ts branch coverage 70.23%** — uncovered branches are error-handling catch blocks (prompt write failure, JSON parse failure). Reasonable to leave untested.
- **LOW (not fixed): coverage-summary.json confusion** — file contains Jest-format results, not istanbul coverage data. Pre-existing.
- **No integration test for full stream-json → Ink pipeline** — all tests mock the renderer.
- **Coverage: 96.65% overall, all 111 files above 80% floor. 2722 tests pass.**

