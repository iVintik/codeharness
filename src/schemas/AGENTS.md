# src/schemas — JSON Schema Definitions

JSON Schema files for validating YAML/JSON configuration formats at parse time. Consumed by `src/lib/schema-validate.ts`.

| File | Purpose |
|------|---------|
| workflow.schema.json | JSON Schema (draft-07) defining valid workflow YAML structure — tasks, flow arrays, loop blocks |
