# Session Issues Log — 2026-03-17

### 15-1-non-sequential-story-selection — dev-story (2026-03-17T02:00Z)

- stories_completed increment may have been missing from original Step 4 — added, but could double-count if handled elsewhere
- stories_skipped counter increments on every Step 2 scan, not deduplicated per story_key — could overcount if same story skipped across multiple scans
- Blocked story info message changed from epic-level to per-story format (behavioral output change)

### 15-2-verification-to-dev-feedback-loop — dev-story (2026-03-17T02:20Z)

None.

### 15-3-retry-state-management — dev-story (2026-03-17T02:30Z)

- Pre-existing TypeScript strict-mode errors (49 across codebase) — not introduced by this story
- Commander's `--story` option prints error to stderr during some test runs (cosmetic)

### 15-3-retry-state-management — code-review (2026-03-17T02:40Z)

- HIGH: --story silently ignored without --reset — FIXED
- HIGH: No input validation on --story key (path traversal) — FIXED
- HIGH: Bare relative 'ralph' path not anchored to cwd — FIXED
- MEDIUM: --status --story had no filtering — FIXED
- LOW (unfixed): run.ts reads .flagged_stories directly instead of shared retry-state.ts module

### 2-1-dependency-auto-install-otlp-instrumentation — dev-story (2026-03-17T07:55Z)

- Build tooling: `npm run build` failed initially because `tsup` was not installed. Required `npm ci` first. Not a code issue, just a local environment state.
- Code quality concern: Re-run path in init.ts now calls `checkInstalled()` which executes real subprocess calls on every re-run. For 3 deps with 15s timeouts, worst case is ~45s if all tools are missing. Acceptable for now but could be optimized.
- The `--no-observability` flag uses Commander's built-in `--no-*` prefix convention, consistent with existing flags.

### 2-1-dependency-auto-install-otlp-instrumentation — dev-story (2026-03-17T11:50Z)

- All three AC fixes (AC 3, 5, 7) were already present in uncommitted source changes from a prior session. No new code written.
- Verification failures were caused by stale globally installed `codeharness` binary (v0.16.1) — verifier tested global binary instead of local build.
- Commander.js `--story` stderr messages during test runs are expected parser output, not failures.

### 2-1-dependency-auto-install-otlp-instrumentation — code-review (2026-03-17T08:10Z)

- HIGH: Missing test coverage for `configureAgent` Python pipx fallback (otlp.ts:195-197) — FIXED
- MEDIUM: Redundant ternary in `installAgentOtlp` (otlp.ts:215) — FIXED
- MEDIUM: Missing test for `configureWeb` with non-nodejs stack — FIXED
- LOW (unfixed): `deps.ts` branch coverage at 82.14% — uncovered branches are cosmetic output suppression paths
- LOW (unfixed): `init.ts` has 6 uncovered lines — error/edge-case paths in init orchestration
- LOW (unfixed): `installAgentOtlp` function appears to be dead code — consider removing
- Architecture note: `configureWeb` creates JS file even for Python stacks

### 2-1-dependency-auto-install-otlp-instrumentation — code-review (2026-03-17T12:10Z)

- HIGH: `patchNodeStartScript` crashes on malformed `package.json` — FIXED
- MEDIUM: `configureWeb` hardcoded `localhost:4318` in generated web snippet — FIXED
- LOW (unfixed): `installAgentOtlp` is dead code
- LOW (unfixed): `configureAgent` writes misleading `agent_sdk: 'traceloop'` for null stack
- LOW (unfixed): `installPythonOtlp` pipx fallback can leave partial installs
- Architecture: `instrumentProject` does 3 extra state file read/write cycles — could use single read-modify-write
