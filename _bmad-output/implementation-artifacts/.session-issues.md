# Session Issues Log — 2026-03-17

### 2-3-observability-querying-agent-visibility-into-runtime — code-review (2026-03-17T16:04Z)

- **MEDIUM bug fixed:** Hook `CLAUDE_PLUGIN_ROOT` fallback missing — could produce `/bin/codeharness` path when env var unset
- **MEDIUM bug fixed:** Hook prompt missing service_name scoping — queries all projects on shared stack
- **MEDIUM bug fixed:** Duplicate code in status.ts `handleFullStatus` local-shared branch
- **LOW unfixed:** VictoriaLogs query URLs use unencoded colons (works in practice, technically incorrect per RFC 3986)
- **LOW unfixed:** Story AC#6 documents flat key `logs_queried` but implementation uses `session_flags.logs_queried`
- **LOW unfixed:** `post-test-verify.sh` emits `[WARN]` to stderr when state file missing
- **Coverage:** status.ts branch coverage at 73.14% (statement 91.06%). No automated integration tests for hook scripts.
- **Architecture concern:** Hook reads YAML state file via `grep 'service_name:'` which is fragile — acceptable tradeoff for 500ms NFR.
