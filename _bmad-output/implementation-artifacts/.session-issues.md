# Session Issues Log — 2026-04-03 (Session 8)

## Prior Sessions (Summary)

Sessions 1-6 completed all Epic 16 stories (16-1 through 16-6). Session 7 completed create-story, dev-story, and code-review for 17-1-worktree-manager. Key recurring issues:
- `_bmad/bmm/config.yaml` path incorrect (actual: `_bmad/config.yaml`)
- `codeharness coverage --min-file 80` broken due to pre-existing stats.test.ts failure
- AGENTS.md consistently missing new file entries
- Pre-existing tsc errors in unrelated files
- Proof format headers frequently wrong on first attempt (`### AC #N:` vs `## AC N:`)

---

### 17-1-worktree-manager — verify (2026-04-03T19:52)

- AGENTS.md was stale (missing worktree-manager.ts entry). Fixed.
- `coverage_met` flag was false in harness state. Fixed by running `codeharness coverage --min-file 80`.
- Proof format: initial proof had `output` blocks without preceding `bash` blocks — proof parser requires `bash`+`output` pairs. Rewrote proof.
- Proof tier format: `Tier: runtime-provable` needed to be `**Tier:** runtime-provable` (bold) for tier detection regex.
- Sprint-status.yaml was reset to `backlog` by `codeharness coverage` command regenerating from sprint-state.json. Manual override to `done`.
- AC 3 runtime test used bash `time` command rather than TypeScript import. Valid but indirect evidence.

## Token Report
- Total tool calls: 15
- By tool: Bash: 11, Read: 3, Write: 1
- Largest Bash outputs: `npm test` (~30 lines), `npx vitest run --coverage` (~30 lines)
- Files read: 3 unique, 3 total reads
- Redundant operations: coverage grep run 3 times due to output truncation

